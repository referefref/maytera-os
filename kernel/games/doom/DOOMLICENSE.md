# DOOM engine — licensing notice

The DOOM engine source in this directory (and the `d_*`, `i_*`, `r_*`, `p_*`,
`w_*`, `z_*`, `m_*`, `g_*`, `s_*`, `v_*` files derived from it) originates from
id Software's DOOM source release. **Every source file carries id Software's
own license header in its preamble** — the *DOOM Source Code License* (Copyright
(C) 1993-1996 id Software, Inc.).

That license is **separate from, and not** the GNU GPL that covers the rest of
MayteraOS. It permits distribution and modification of this source under id's
terms (non-commercial). It does **not** place this code under the GPL.

If you redistribute this subtree, keep the per-file id Software headers intact.
The authoritative license text is the one published by id Software and reproduced
in the file headers here. MayteraOS's own code (everything outside this DOOM
subtree and the derived `*_doom` glue) remains GPLv2-or-later.
