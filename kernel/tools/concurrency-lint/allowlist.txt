# concurrency-lint allowlist (#426, re-baselined at #514 2026-08-02).
#
# Line format:
#   <fingerprint>  # <path>:<line> <RULE> [CATEGORY] <why this site is acceptable>
#
# Fingerprint = path|rule|sha1(normalized-snippet)[:12]. It is stable across
# line-number drift, so unrelated edits do not spuriously trip the lint.
#
# CATEGORIES (the lint REJECTS a line with no category or no real justification,
# and REJECTS a line whose fingerprint matches nothing in the tree, so this file
# cannot silently grow and cannot rot into dead hashes):
#   [LEGIT]  individually reviewed, correct by design. Do not "fix" these.
#   [DEBT]   individually reviewed, accepted for now, SHOULD become a wait_event.
#            Printed as a loud ledger on every build.
#   [LEGACY] bulk-baselined at #514, NOT individually reviewed. #514 fixed a bug
#            where COND_PROGRESS_RE matched the second `=` of `==`, so every
#            `while (x == 0);` busy-wait, and every poll hidden behind a
#            one-line delay wrapper, was invisible. Fixing it took the kernel
#            count from 12 to 179. Those 167 newly-visible sites are recorded
#            here as a BACKLOG so the gate can go green and start catching NEW
#            violations today. They are not endorsed.
#
# NEVER add a [LEGACY] line by hand. A new hit is either fixed, or reviewed into
# [LEGIT]/[DEBT] with a real reason. Regenerate only with --update-allowlist,
# which writes TODO notes the lint then rejects until a human fills them in.

kernel/cpu/apic.c|DELAY_SPIN|75bcf7bc376c  # kernel/cpu/apic.c:209 DELAY_SPIN [DEBT] REVIEWED #426ph2. lapic_wait_ipi_idle() polls ICR delivery-status with pause(). A spin is architecturally right (delivery takes microseconds and raises no interrupt) but the loop is UNBOUNDED: a wedged LAPIC hangs the kernel here forever. Needs an iteration cap and a loud give-up, not a wait queue.
kernel/cpu/apic.c|DELAY_SPIN|c05b804b1ce4  # kernel/cpu/apic.c:300 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: lapic_delay_us <- lapic_timer_calibrate <- lapic_timer_init/lapic_timer_oneshot, all with zero callers; the kernel is still on the PIT. Bound if revived is us*1000 pause() iterations.
kernel/cpu/smp.c|DELAY_SPIN|633863a7d72e  # kernel/cpu/smp.c:387 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. AP INIT-IPI settle, fixed 10,000,000 pause() iterations inside start_ap(), which runs pre-scheduler with no thread to sleep. Additionally unreachable today: main.c:593 gates smp_start_aps() on g_smp_user_sched, which is 0 and never assigned anywhere.
kernel/cpu/smp.c|DELAY_SPIN|057dd3c5c613  # kernel/cpu/smp.c:395 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. AP SIPI settle, fixed 200,000 pause() iterations in start_ap(), pre-scheduler, same g_smp_user_sched gate as smp.c:387.
kernel/cpu/smp.c|DELAY_SPIN|f1134c6a0683  # kernel/cpu/smp.c:403 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. AP start handshake, hard cap 1,000,000 pause() with an early exit on tdata->started; AP bring-up only, no scheduler exists to wait on.
kernel/cpu/smp.c|DELAY_SPIN|958f1c99d113  # kernel/cpu/smp.c:442 DELAY_SPIN [DEBT] REVIEWED #287pass 2026-08-03. smp_wait_for_aps() has ZERO callers today, but its loop is an UNBOUNDED while (state != want && state != HALTED) pause() that would hang the kernel forever on an AP that neither reaches the state nor halts. It must gain an iteration cap and a loud give-up BEFORE anyone wires it up.
kernel/cpu/smp.c|DELAY_SPIN|50242ce95759  # kernel/cpu/smp.c:852 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Contended path of bkl_acquire/bkl_reacquire, unreachable in the shipping build (APs never start; on one CPU bkl_owner==cpu short-circuits before the spin). Correct IRQ-friendly spinlock as written: sti before the spin-read, cli before the CAS/publish.
kernel/cpu/smp.c|DELAY_SPIN|50242ce95759  # kernel/cpu/smp.c:904 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Contended path of bkl_acquire/bkl_reacquire, unreachable in the shipping build (APs never start; on one CPU bkl_owner==cpu short-circuits before the spin). Correct IRQ-friendly spinlock as written: sti before the spin-read, cli before the CAS/publish.
kernel/dos/dosexec.c|YIELD_SPIN|7ae26db38d85  # kernel/dos/dosexec.c YIELD_SPIN [LEGIT] REVIEWED #dospace 2026-08-07. THE DOS INTERPRETER RUN LOOP, NOT A WAKE-WAIT. It waits for nothing: every pass executes at least DOS_SLICE_MIN (20,000) guest instructions of real forward progress, so it cannot spin on a wake that never arrives, and the whole run is bounded by DOS_MAX_RUN_MS. The proc_yield() is a deliberate scheduler HANDOFF after each burst, not a poll: it replaced a fixed proc_sleep(5) that cost the guest half its instruction rate while the machine sat 38-45% idle (measured, build 1730). The DOS proc runs at PRIO_NORMAL so the yield really does hand off to a runnable peer. Converting this to wait_event() would be wrong: there is no condition to wait for.
kernel/drivers/ac97.c|DELAY_SPIN|192b31060b39  # kernel/drivers/ac97.c:73 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. ac97_delay calibrated delay helper, bound us*10 io_wait() (io_wait is outb 0x80, ~1us, so ~10x the nominal microseconds). Worst context is runtime via ac97_stop <- audio_stop, but it is a fixed-count hardware settle with no wake source that could exist.
kernel/drivers/ac97.c|SLEEP_POLL|82d6ee80572c  # kernel/drivers/ac97.c:84 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. ac97_codec_wait is marked __attribute__((unused)) and has zero callers. Bound if revived is 1000 iterations x ac97_delay(10), about 100ms.
kernel/drivers/ac97.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/ac97.c:167 SLEEP_POLL [DEBT] REVIEWED #287pass 2026-08-03. ac97_reset_codec is capped at 100 x ac97_delay(10000), about 10s, on the deferred PRIO_LOW audioinit thread; ac97_stop is capped at 100 x ac97_delay(100), about 100ms, on the runtime media-stop path. Bounded, but 10s of non-yielding CPU burn at init should become wait_event_timeout.
kernel/drivers/ac97.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/ac97.c:471 SLEEP_POLL [DEBT] REVIEWED #287pass 2026-08-03. ac97_reset_codec is capped at 100 x ac97_delay(10000), about 10s, on the deferred PRIO_LOW audioinit thread; ac97_stop is capped at 100 x ac97_delay(100), about 100ms, on the runtime media-stop path. Bounded, but 10s of non-yielding CPU burn at init should become wait_event_timeout.
kernel/drivers/acpi.c|DELAY_SPIN|3564dd8ff378  # kernel/drivers/acpi.c:508 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Hard cap 100,000 io_wait() (about 100ms) on the shutdown fallback taken after the parsed ACPI S5 write failed. The machine is powering off, so there is no user-visible work left for this to stall.
kernel/drivers/acpi.c|DELAY_SPIN|de2a6e64bcb8  # kernel/drivers/acpi.c:544 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Two hard-capped 1,000,000-iteration io_wait() waits (about 1s each) after the ACPI reset-register write and after the 8042 KB_RESET_CMD, on the reboot path. Deliberately runs with IF=0 (cli at acpi.c:521) and ends in an intentional triple fault.
kernel/drivers/acpi.c|DELAY_SPIN|de2a6e64bcb8  # kernel/drivers/acpi.c:565 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Two hard-capped 1,000,000-iteration io_wait() waits (about 1s each) after the ACPI reset-register write and after the 8042 KB_RESET_CMD, on the reboot path. Deliberately runs with IF=0 (cli at acpi.c:521) and ends in an intentional triple fault.
kernel/drivers/ahci.c|DELAY_SPIN|d10b76b4960d  # kernel/drivers/ahci.c:47 DELAY_SPIN [DEBT] REVIEWED #287pass 2026-08-03. ahci_delay(1) is 20,000 outb 0x80 round trips (about 20ms real, each a VM exit under virtualization). #287 removed it from the three ERROR-RECOVERY loops (port_stop_cmd, port_start_cmd, PxCMD.CLO), which ran with interrupts off under g_ata_io_lock; the remaining callers are boot-time port spin-up and probe, where a fixed settle is correct. Should still adopt the shared mono_us() clock.
kernel/drivers/ahci.c|SLEEP_POLL|86e33b442f08  # kernel/drivers/ahci.c:184 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. BSY/DRQ wait in port_init, reachable only via ahci_init_one <- ahci_init <- ata_init <- main.c:396, all before sti(). Hard cap 500 x ahci_delay(1), about 10s of boot time worst case.
kernel/drivers/ahci.c|SLEEP_POLL|d6e543489fda  # kernel/drivers/ahci.c:560 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. BIOS/OS handoff (BOHC) wait in ahci_init_one, hard cap 25 x ahci_delay(1), about 500ms, boot probe only.
kernel/drivers/ahci.c|SLEEP_POLL|b1837883e7d6  # kernel/drivers/ahci.c:585 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 32-iteration walk over implemented ports in ahci_init_one; the loop body contains no wait of its own, the delay it encloses is the PxSSTS.DET spin-up wait below.
kernel/drivers/ahci.c|SLEEP_POLL|71ffa3656bb0  # kernel/drivers/ahci.c:610 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. PxSSTS.DET / PxSIG spin-up wait, hard cap 30 x ahci_delay(1), about 600ms per implemented port, boot probe only.
kernel/drivers/ata.c|DELAY_SPIN|5662c18f495f  # kernel/drivers/ata.c:433 DELAY_SPIN [LEGIT] REVIEWED #426ph2. `for (i=0;i<10;i++) io_wait();` - a fixed 10-iteration (~4us) hardware settle after an ATA soft reset, during probe, before the scheduler exists. Bounded by construction, no wake source exists to wait on, and the ATA spec requires a delay here. Correct as written; do not convert.
kernel/drivers/audio.c|SLEEP_POLL|82d6ee80572c  # kernel/drivers/audio.c:566 SLEEP_POLL [DEBT] Bounded 1000-iteration drain wait for the sink to empty. No wake source is armed on drain completion; should become wait_event_timeout against the HDA BCIS wake (#426).
kernel/drivers/audio.c|SLEEP_POLL|ce484f8b87c2  # kernel/drivers/audio.c:956 SLEEP_POLL [LEGIT] Real-time audio pacing: the sleep paces the decoder to the wall clock and releases the BKL. It is not waiting for a wake, so a wait queue is the wrong shape.
kernel/drivers/audio.c|SLEEP_POLL|e1846f247b7f  # kernel/drivers/audio.c:961 SLEEP_POLL [LEGIT] Same real-time pacing loop as audio.c:956, the behind-realtime branch. Sleeping IS the intended behaviour here, not a substitute for a wake.
kernel/drivers/hda.c|DELAY_SPIN|192b31060b39  # kernel/drivers/hda.c:103 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. hda_delay calibrated delay helper, bound us*10 io_wait() (about 10x the nominal microseconds). Reached at runtime via hda_stop and hda_configure_output_stream, worst enclosing cost about 300ms; a fixed hardware settle with no wake source.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:119 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:131 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|ea827b9cac9b  # kernel/drivers/hda.c:170 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. CORBCTL.RUN stop wait, hard cap 100 x hda_delay(100), about 100ms. hda_setup_corb_rirb is reached only from hda_init and the hda_devlog_scan restore, both inside the deferred PRIO_LOW audioinit thread.
kernel/drivers/hda.c|SLEEP_POLL|a918dea808fe  # kernel/drivers/hda.c:176 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. RIRBCTL.RUN stop wait, identical shape and identical 100 x hda_delay(100) cap, about 100ms, same audioinit-thread-only reach.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:200 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:205 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|35b579cbce9f  # kernel/drivers/hda.c:265 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Hard cap g_hda_cmd_max_iters (default 2000) x hda_delay(10), about 200ms per codec verb; normally returns on the first or second iteration. Runs on the audioinit worker (BKL dropped) or a Ring-3 SYS_HDA_DBG call, both preemptible, so it burns its own timeslice rather than freezing the UI. Note SYS_HDA_DBG is an ungated Ring-3 lever to pin a core in Ring 0 for about 200ms per call; that is a separate security ticket, not a #426 one.
kernel/drivers/hda.c|SLEEP_POLL|ba5904331062  # kernel/drivers/hda.c:536 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Bounded by nroutes <= HDA_MAX_OUT_ROUTES == 4; each route issues about 8 codec verbs plus hda_delay(1000)/hda_delay(10000), on the deferred audioinit worker with the BKL dropped.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:695 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:704 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:712 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|943d2487fc6d  # kernel/drivers/hda.c:1011 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 60 iterations x hda_delay(2000) (about 1.2s) then 100 x hda_delay(100) (about 100ms); single caller is hda_init(), which runs exactly once from the deferred boot worker with the BKL released around it.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:1019 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/hda.c:1311 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Every site is a hard 100-iteration cap x hda_delay(100), about 100ms (hda.c:1019 is 60 x hda_delay(2000), about 1.2s). Worst context is runtime via hda_stop <- audio_stop and hda_configure; the rest run one-shot on the deferred PRIO_LOW audioinit thread, which drops the BKL around audio_init.
kernel/drivers/hda.c|SLEEP_POLL|24be625b06be  # kernel/drivers/hda.c:1480 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Hard cap 40 x hda_delay(3000), about 1.2s, and unreachable at runtime: both callers (audio_beep and hda_selftest_worker) are themselves dead, since sound_panel_register() and audio_start_hda_selftest() have zero callers.
kernel/drivers/intel_gpu.c|DELAY_SPIN|192b31060b39  # kernel/drivers/intel_gpu.c:47 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: drivers/intel_gpu.c is compiled and linked but nothing in the tree references any intel_gpu_ symbol. Bound if revived is caller-supplied us*10 io_wait().
kernel/drivers/intel_gpu.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/intel_gpu.c:222 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_gpu.c has zero callers). Each loop is 100 iterations x intel_delay(100), about 100ms, with an explicit NAK early-out.
kernel/drivers/intel_gpu.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/intel_gpu.c:236 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_gpu.c has zero callers). Each loop is 100 iterations x intel_delay(100), about 100ms, with an explicit NAK early-out.
kernel/drivers/intel_gpu.c|SLEEP_POLL|3564dd8ff378  # kernel/drivers/intel_gpu.c:792 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_gpu.c has zero callers). Hard cap 100000 x intel_delay(1), about 1s, then logs a BLT wait timeout and returns.
kernel/drivers/intel_hda.c|DELAY_SPIN|192b31060b39  # kernel/drivers/intel_hda.c:63 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: drivers/intel_hda.c is a duplicate, superseded HD Audio driver; intel_hda_init() has zero callers and the live driver is drivers/hda.c. Bound if revived is caller-supplied us*10 io_wait(). Delete the file rather than convert it.
kernel/drivers/intel_hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/intel_hda.c:150 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). Two GCTL.CRST loops, each 100 x ihda_delay(100), about 100ms.
kernel/drivers/intel_hda.c|SLEEP_POLL|19af7c3ed938  # kernel/drivers/intel_hda.c:162 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). Two GCTL.CRST loops, each 100 x ihda_delay(100), about 100ms.
kernel/drivers/intel_hda.c|SLEEP_POLL|186e932f0a25  # kernel/drivers/intel_hda.c:211 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). 100 iterations x ihda_delay(100), about 100ms, waiting for CORBRP[15] to set.
kernel/drivers/intel_hda.c|SLEEP_POLL|e9efac9892eb  # kernel/drivers/intel_hda.c:215 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). 100 iterations x ihda_delay(100), about 100ms, waiting for CORBRP[15] to clear.
kernel/drivers/intel_hda.c|SLEEP_POLL|82d6ee80572c  # kernel/drivers/intel_hda.c:251 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). 1000 iterations x ihda_delay(10), about 100ms per codec verb, then logs a timeout and returns 0xFFFFFFFF.
kernel/drivers/intel_hda.c|SLEEP_POLL|d871ee9e06d6  # kernel/drivers/intel_hda.c:663 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). SD_CTL.RUN clear wait, 100 x ihda_delay(100), about 100ms.
kernel/drivers/intel_hda.c|SLEEP_POLL|407bc88db152  # kernel/drivers/intel_hda.c:672 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). SD_CTL.SRST set wait, 100 x ihda_delay(100), about 100ms.
kernel/drivers/intel_hda.c|SLEEP_POLL|a35df1e79445  # kernel/drivers/intel_hda.c:679 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). SD_CTL.SRST clear wait, 100 x ihda_delay(100), about 100ms.
kernel/drivers/intel_hda.c|SLEEP_POLL|d871ee9e06d6  # kernel/drivers/intel_hda.c:908 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (intel_hda.c has zero callers). SD_CTL.RUN clear wait, 100 x ihda_delay(100), about 100ms.
kernel/drivers/net/wifi/rtl8812bu.c|SLEEP_POLL|a7d569cb720d  # kernel/drivers/net/wifi/rtl8812bu.c:1248 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Compiled out, not merely uncalled: the single call site is inside #if RTL8822B_INIT_LCK, which defaults to 0, hence the __attribute__((unused)). Bound if enabled is 8 iterations x proc_sleep(10), about 80ms of yielding sleep.
kernel/drivers/net/wifi/rtl8812bu.c|SLEEP_POLL|4c69d0084e8c  # kernel/drivers/net/wifi/rtl8812bu.c:2494 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 13 iterations (channels 1..13) on the dedicated PRIO_HIGH wifiscan worker; per channel about 13ms of busy PIT delay plus rtl_iqk_run, so about 170ms total, and only when an RTL8812BU/8822BU dongle enumerated.
kernel/drivers/net/wifi/rtl8812bu.c|SLEEP_POLL|6f821e1ee0ee  # kernel/drivers/net/wifi/rtl8812bu.c:2525 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Hard cap of 40 attempts with an early break after 4 consecutive idle reads; each xhci_bulk_transfer carries xhci_wait_transfer's 5000ms budget, and the thread BLOCKS via xhci_may_block/xhci_block_for_cc rather than spinning. Slow scanning on a dedicated worker, not CPU burn or a UI stall.
kernel/drivers/sound.c|DELAY_SPIN|5bcb138935bf  # kernel/drivers/sound.c:35 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. delay_us calibrated helper, bound us*10 io_wait(). Live only via sound_init -> dsp_reset, which runs once on the deferred PRIO_LOW audioinit worker.
kernel/drivers/sound.c|SLEEP_POLL|82d6ee80572c  # kernel/drivers/sound.c:54 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. dsp_reset is capped at 1000 x delay_us(10) (about 100ms) per probed base port; dsp_write/dsp_read are capped at 1000 x delay_us(1) (about 10ms) each. All on the one-shot deferred audio-init worker.
kernel/drivers/sound.c|SLEEP_POLL|82d6ee80572c  # kernel/drivers/sound.c:71 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. dsp_reset is capped at 1000 x delay_us(10) (about 100ms) per probed base port; dsp_write/dsp_read are capped at 1000 x delay_us(1) (about 10ms) each. All on the one-shot deferred audio-init worker.
kernel/drivers/sound.c|SLEEP_POLL|82d6ee80572c  # kernel/drivers/sound.c:84 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. dsp_reset is capped at 1000 x delay_us(10) (about 100ms) per probed base port; dsp_write/dsp_read are capped at 1000 x delay_us(1) (about 10ms) each. All on the one-shot deferred audio-init worker.
kernel/drivers/sound.c|SLEEP_POLL|397c62572519  # kernel/drivers/sound.c:281 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Bounded by total_samples = 22050*duration_ms/1000 with delay_us(45) per sample, so a 500ms tone is about 5s of hard non-yielding spin, but the only live caller is the beep command in kernel_shell(), which main.c documents as reachable ONLY on a machine with no framebuffer, so there is no UI to block. LATENT: becomes an immediate multi-second freeze if beep is ever surfaced to the GUI.
kernel/drivers/sound.c|DELAY_SPIN|4b84c16963f7  # kernel/drivers/sound.c:320 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. PC-speaker branch, bound duration_ms x delay_us(1000), so about 10x the requested duration (a 500ms request is about 5s). Same headless-only kernel_shell reachability as sound.c:281, and the same latent risk if beep reaches the GUI.
kernel/drivers/usb_audio.c|SLEEP_POLL|fd417bc6fee9  # kernel/drivers/usb_audio.c:622 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Not a spin: each pass ends in proc_sleep(8) and the loop is bounded by timer_ticks < end (duration_ms). Runs on the dedicated UAC tone worker, reached only via uac_play_tone_continuous, which is gated behind /CONFIG/UACTONE.CFG.
kernel/drivers/usb_audio.c|SLEEP_POLL|42f599e23feb  # kernel/drivers/usb_audio.c:729 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Not a spin: the live music-playback pump, proc_sleep(8) per pass, bounded by !eof && timer_ticks < end (max_ms backstop), on its own PCM pump thread that yields every batch, so it cannot block the compositor.
kernel/drivers/usb_hid.c|SLEEP_POLL|c837787f6655  # kernel/drivers/usb_hid.c:509 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Not a spin: a 250Hz cooperative worker thread doing only non-blocking xhci_int_in_poll/submit per pass then proc_sleep(4). It is also load-bearing as one of the two always-armed xHCI event-ring drainers (#614); removing it would weaken every xhci_block_for_cc waiter.
kernel/drivers/usb_msc.c|YIELD_SPIN|847c474c9714  # kernel/drivers/usb_msc.c:222 YIELD_SPIN [LEGIT] RETIRED AS DEBT BY #617. msc_cmd_lock() is no longer a hand-rolled sleeping mutex: it acquires uncontended with one atomic, and on contention parks on g_msc_cmd_wq via __wait_prepare/__wait_event_wait_deadline, woken by wake_up_all() in msc_cmd_unlock(). The prepare-then-recheck order closes the lost-wake window, and the 10 ms per-park cap is a re-poll quantum, NOT a give-up timeout, so liveness is identical to the old poll with zero CPU burn. What REMAINS at this fingerprint is only msc_cmd_lock_noblock(), the fallback for a context wq_may_block() says cannot park (pre-scheduler / no proc / IF=0). There it is correct by necessity: with no scheduler there is nothing to park onto. It is also unreachable on contention by construction (the USB stack is single-threaded before the scheduler exists, so the uncontended fast path always wins), and #617 does not merely assert that - g_msc_noblock_spins counts every iteration and warns on serial the first time, so if the claim is ever wrong we find out instead of guessing.
kernel/drivers/xhci.c|SLEEP_POLL|c837787f6655  # kernel/drivers/xhci.c:1407 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. The two wait loops are the already-remediated #375/#525/#619 shape: a real-time deadline off the TSC monotonic clock (not timer_ticks), a 1ms fine poll window, then xhci_may_block() -> xhci_block_for_cc(), a genuine wait_event capped at XHCI_BLOCK_MS 10ms, with proc_sleep(1) as the no-block-context fallback; overall budget 5000ms per bulk transfer. The other two are the rescan/event worker threads. NOTE: the recorded line numbers have drifted.
kernel/drivers/xhci.c|SLEEP_POLL|c837787f6655  # kernel/drivers/xhci.c:1465 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. The two wait loops are the already-remediated #375/#525/#619 shape: a real-time deadline off the TSC monotonic clock (not timer_ticks), a 1ms fine poll window, then xhci_may_block() -> xhci_block_for_cc(), a genuine wait_event capped at XHCI_BLOCK_MS 10ms, with proc_sleep(1) as the no-block-context fallback; overall budget 5000ms per bulk transfer. The other two are the rescan/event worker threads. NOTE: the recorded line numbers have drifted.
kernel/drivers/xhci.c|SLEEP_POLL|c837787f6655  # kernel/drivers/xhci.c:3420 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. The two wait loops are the already-remediated #375/#525/#619 shape: a real-time deadline off the TSC monotonic clock (not timer_ticks), a 1ms fine poll window, then xhci_may_block() -> xhci_block_for_cc(), a genuine wait_event capped at XHCI_BLOCK_MS 10ms, with proc_sleep(1) as the no-block-context fallback; overall budget 5000ms per bulk transfer. The other two are the rescan/event worker threads. NOTE: the recorded line numbers have drifted.
kernel/exec/win16api.c|SLEEP_POLL|9c6f4762b991  # kernel/exec/win16api.c:11279 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Pacing, not a wake: proc_sleep(8) every iteration with a hard cap of hz*180 ticks, and it runs on its own win16 kernel proc, so a modal Win16 dialog paces the emulated app and never touches the compositor.
kernel/gui/clock.c|SPIN_BUSYWAIT|1400381f5357  # kernel/gui/clock.c:75 SPIN_BUSYWAIT [DEBT] REVIEWED #426ph2. `while (rtc_update_in_progress());` - an UNBOUNDED busy-wait on the RTC UIP bit with no iteration cap. Normally clears in under 2ms, but an RTC that never clears UIP hangs the kernel forever with interrupts on and a core pegged. There is no interrupt to wait for here, so the right fix is a BOUND (counted loop + give up and return the last good time), not a wait queue. Small and safe; next pass.
kernel/gui/clock.c|SPIN_BUSYWAIT|1400381f5357  # kernel/gui/clock.c:104 SPIN_BUSYWAIT [DEBT] REVIEWED #426ph2. Second copy of the same unbounded RTC UIP spin (rtc_read_date). Same verdict and same fix as clock.c:75; they should share one bounded helper rather than being two copies.
kernel/gui/desktop.c|YIELD_SPIN|8779bc817025  # kernel/gui/desktop.c:3116 YIELD_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Pacing, not a wake: this is the desktop frame loop and every path out of the body sleeps (proc_sleep(50) when the compositor owns the screen, 33 under the screensaver, 8/30 on the frame tail). Already fixed for #102/#180.
kernel/gui/installer.c|SLEEP_POLL|95083efd4741  # kernel/gui/installer.c:982 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Bounded by part_sectors/64 iterations that each strictly decrement remaining and bail on I/O error; the proc_sleep(1) fires once per 4096 sectors (about 2MB) as a courtesy yield, and the only live caller is the headless AUTOINST worker on its own proc.
kernel/gui/login.c|YIELD_SPIN|c5bebca4e25a  # kernel/gui/login.c:1017 YIELD_SPIN [DEBT] REVIEWED #287pass 2026-08-03. HARMFUL. login_run()'s while(1) { ...; login_draw(); proc_yield(); } has no sleep, so the login/logout screen pegs a core at 100% and redraws the framebuffer flat out for as long as nobody types. Unbounded, and it is the first screen a user sees. This is the exact #102/#180 pattern desktop.c already fixed with proc_sleep; the fix is the same one line.
kernel/gui/properties.c|SPIN_BUSYWAIT|bd8cc0686b35  # kernel/gui/properties.c:944 SPIN_BUSYWAIT [DEBT] REVIEWED #287pass 2026-08-03. HARMFUL. properties_run() burns for (volatile i < 100000) with NO sleep or yield for the entire life of the dialog, on the kernel desktop thread, so the taskbar, other windows and that thread's net_poll all stop while Properties is open. Only reachable through the KERNEL file browser (the shipping golden runs userland /apps/files), which is what contains it today. One-line fix: proc_sleep(16).
kernel/gui/terminal.c|SLEEP_POLL|7c5482e8dd79  # kernel/gui/terminal.c:699 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Same shape on the dedicated term_pump proc: drain without sleeping when there is data, else proc_sleep(15), with child-death and POLL_HUP exits. 15ms latency and about 66 wakeups/s per open terminal, no hang.
kernel/io/async_io.c|DELAY_SPIN|e5bbd7035e64  # kernel/io/async_io.c:46 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: a standard TTAS spinlock private to io/async_io.c, and every async_io entry point (aio_init, aio_ring_create, aio_ring_destroy and friends) has zero callers anywhere in kernel or userland. The io_uring-style subsystem was written and never wired up.
kernel/io/async_io.c|SLEEP_POLL|9083a80e18d8  # kernel/io/async_io.c:200 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (async_io has zero callers), and bounded anyway: timeout=1000 decremented every pass at about 1ms per pass, so a hard 1s cap before it frees the ring regardless.
kernel/io/async_io.c|DELAY_SPIN|82d6ee80572c  # kernel/io/async_io.c:202 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (async_io has zero callers). The delay itself is a fixed 1000 x io_wait(), about 1ms per pass of the loop above.
kernel/ipc/msg.c|YIELD_SPIN|c5bebca4e25a  # kernel/ipc/msg.c:359 YIELD_SPIN [DEBT] REVIEWED #426ph2. Already reasoned in-code: converting this proc_yield spin to a conn->recv_wq is BLOCKED on #515 connection lifetime - msg_cleanup_process() and sys_msg_destroy_channel() kfree(chan->server_conn) with no refcount and no wake, so a wait queue inside it would be freed under a parked waiter and __wait_finish() would take a spinlock on freed memory (a UAF WRITE, worse than the UAF READ the spin already does). Fix #515 first. This is a real review, not a bulk baseline.
kernel/main.c|DELAY_SPIN|8ce20ae6af06  # kernel/main.c:349 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Fixed for (j < 500000) io_wait() pacing delays, about 0.5s: one is the boot spinner in kernel_main (pre-desktop), the other is the kernel_shell gui command splash, and kernel_shell is documented as reachable only with no framebuffer at all. NOTE: the recorded line numbers are about 34 lines low; the lint reports positions in its comment-stripped copy of main.c.
kernel/main.c|SPIN_BUSYWAIT|f1f725e321ef  # kernel/main.c:460 SPIN_BUSYWAIT [LEGIT] #307 USB MSC root-mount retry backoff. Runs with interrupts OFF before the scheduler exists, so there is literally no wait primitive to use yet.
kernel/main.c|DELAY_SPIN|ff72061e9f6d  # kernel/main.c:1331 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: inside the boot outbound-ping self-test that main.c wraps in if (0) {, so this fixed about 5ms ARP backoff is compiled but never executed. NOTE: recorded line number is about 34 low (lint reports comment-stripped positions).
kernel/main.c|DELAY_SPIN|16e54a1b3cb9  # kernel/main.c:1344 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: same if (0) boot ping block, a fixed about 2ms delay inside a 200-iteration reply poll. NOTE: recorded line number is about 34 low.
kernel/main.c|DELAY_SPIN|017294c24ea4  # kernel/main.c:1768 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Inside #ifdef SECTEST_CV_FETCH, built only by make CVTEST=1 for the #510 verification kernel, never in the golden. Boot-time DHCP settle with hard caps of 4000 and 400 outer iterations. NOTE: recorded line numbers are about 34 low.
kernel/main.c|DELAY_SPIN|017294c24ea4  # kernel/main.c:1776 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Inside #ifdef SECTEST_CV_FETCH, built only by make CVTEST=1 for the #510 verification kernel, never in the golden. Boot-time DHCP settle with hard caps of 4000 and 400 outer iterations. NOTE: recorded line numbers are about 34 low.
kernel/main.c|DELAY_SPIN|c6b5f2d14edf  # kernel/main.c:2126 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. kernel_shell ping/dhcp handlers: fixed about 1ms io_wait() delays inside outer loops capped at 200 (reply wait), 50 (ARP wait) and the DHCP wait, all with a ^C check. kernel_shell is headless-serial-only, so there is no GUI to freeze. NOTE: recorded line numbers are about 34 low.
kernel/main.c|DELAY_SPIN|c6b5f2d14edf  # kernel/main.c:2191 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. kernel_shell ping/dhcp handlers: fixed about 1ms io_wait() delays inside outer loops capped at 200 (reply wait), 50 (ARP wait) and the DHCP wait, all with a ^C check. kernel_shell is headless-serial-only, so there is no GUI to freeze. NOTE: recorded line numbers are about 34 low.
kernel/main.c|DELAY_SPIN|c6b5f2d14edf  # kernel/main.c:2216 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. kernel_shell ping/dhcp handlers: fixed about 1ms io_wait() delays inside outer loops capped at 200 (reply wait), 50 (ARP wait) and the DHCP wait, all with a ^C check. kernel_shell is headless-serial-only, so there is no GUI to freeze. NOTE: recorded line numbers are about 34 low.
kernel/main.c|DELAY_SPIN|c6b5f2d14edf  # kernel/main.c:2267 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. kernel_shell ping/dhcp handlers: fixed about 1ms io_wait() delays inside outer loops capped at 200 (reply wait), 50 (ARP wait) and the DHCP wait, all with a ^C check. kernel_shell is headless-serial-only, so there is no GUI to freeze. NOTE: recorded line numbers are about 34 low.
kernel/main.c|DELAY_SPIN|1ab8f0e524cd  # kernel/main.c:2500 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Pacing, not a wake: the gui command boot-splash progress bar, 11 steps x a fixed about 50ms delay, headless-shell-only. NOTE: recorded line number is about 34 low.
kernel/main.c|DELAY_SPIN|8ce20ae6af06  # kernel/main.c:2503 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Fixed for (j < 500000) io_wait() pacing delays, about 0.5s: one is the boot spinner in kernel_main (pre-desktop), the other is the kernel_shell gui command splash, and kernel_shell is documented as reachable only with no framebuffer at all. NOTE: the recorded line numbers are about 34 lines low; the lint reports positions in its comment-stripped copy of main.c.
kernel/main.c|DELAY_SPIN|9a22450ddf47  # kernel/main.c:2514 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Pacing, not a wake: the splash command progress bar, 21 steps x a fixed about 30ms delay, headless-shell-only. NOTE: recorded line number is about 34 low.
kernel/net/arp.c|DELAY_SPIN|00e14e3b9b48  # kernel/net/arp.c:425 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Hard caps in source: 2 probes x 1500 guard iterations x a fixed about 200us delay, so about 0.3-0.6s per address, on the netmon kernel proc. The .200-.209 sweep can call it 10 times, so up to about 3-6s of background-thread CPU burn after a DHCP failure; off the UI thread.
kernel/net/dhcp.c|DELAY_SPIN|00e14e3b9b48  # kernel/net/dhcp.c:656 DELAY_SPIN [DEBT] REVIEWED #287pass 2026-08-03. HARMFUL. dhcp_discover_blocking() is not boot-only: SYS_NET_DHCP is called straight from a mouse-click handler in the shipping Settings app and from libc aiclient.c. This while (!nic_link_up()) busy poll therefore runs synchronously inside a GUI app's syscall, so with the cable out Settings is frozen and burning a core for about 10s. The correct fix is the redundant-wake pattern (carrier IRQ plus the existing poll worker feeding a wait_event_timeout), not a smaller delay constant.
kernel/net/dhcp.c|DELAY_SPIN|7c193b4730e9  # kernel/net/dhcp.c:669 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. One-shot fixed about 50ms switch-settle delay after link-up on the SYS_NET_DHCP path; bounded and short enough not to be user-visible.
kernel/net/dhcp.c|DELAY_SPIN|19af7c3ed938  # kernel/net/dhcp.c:718 DELAY_SPIN [DEBT] REVIEWED #287pass 2026-08-03. HARMFUL. Same Settings/aiclient -> SYS_NET_DHCP caller path as dhcp.c:656: the fixed about 100us delay is the entire wait mechanism of the DORA loop (max_iterations 100000), so a no-server network freezes the calling app for a further about 10s of 100% busy poll on top of the link wait. Same redundant-wake fix.
kernel/net/dns.c|SLEEP_POLL|2179b54437e9  # kernel/net/dns.c:415 SLEEP_POLL [DEBT] net_poll()-driven wait on a REMOTE DNS server. A timeout is correct semantics per CLAUDE.md tier 2, but it should be wait_event_timeout, not proc_sleep(2) (#426).
kernel/net/e1000.c|DELAY_SPIN|b10caeaaa71e  # kernel/net/e1000.c:279 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Fixed about 10ms settle immediately after writing CTRL.RST in e1000_init(), which is called only from net_init() at boot. Device probe, no wake source exists yet.
kernel/net/e1000.c|DELAY_SPIN|c695b6a7d5e6  # kernel/net/e1000.c:285 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Same boot probe; hard cap timeout=1000 decremented per io_wait() pass (about 1ms total) waiting for CTRL.RST to self-clear, then it proceeds regardless.
kernel/net/e1000_mq.c|DELAY_SPIN|39eedda1f67e  # kernel/net/e1000_mq.c:249 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: e1000_mq_init() has zero callers and nothing outside e1000_mq.c/.h references the multiqueue E1000 driver. Bound if revived is a probe-time RXDCTL.ENABLE poll, timeout=1000 x io_wait(), about 1ms.
kernel/net/e1000_mq.c|DELAY_SPIN|89d2e4afbc89  # kernel/net/e1000_mq.c:282 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (e1000_mq has zero callers). TXDCTL.ENABLE poll, timeout=1000 x io_wait(), about 1ms, falls through on expiry.
kernel/net/e1000_mq.c|DELAY_SPIN|b10caeaaa71e  # kernel/net/e1000_mq.c:646 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (e1000_mq has zero callers), and by construction a PCI-probe post-CTRL.RST settle: fixed about 10ms, followed by a timeout=1000 reset-complete poll.
kernel/net/e1000_mq.c|DELAY_SPIN|08254c640181  # kernel/net/e1000_mq.c:650 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (e1000_mq_init has zero callers anywhere in the tree). Hard cap timeout=1000 decremented per iteration around io_wait(), about 1ms total.
kernel/net/ftp.c|SLEEP_POLL|e853b3932ab2  # kernel/net/ftp.c:112 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: the whole FTP client is unreachable (nothing calls ftp_connect/ftp_ls/ftp_get/ftp_put; netfs only string-matches the prefix). Bounded anyway: ftp_wait_connect uses the caller's timeout_ms, always FTP_CONNECT_TIMEOUT_MS 10000ms, proc_sleep(1) per iteration.
kernel/net/ftp.c|SLEEP_POLL|cf6df1108870  # kernel/net/ftp.c:141 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (FTP client has no callers). FTP_RESPONSE_TIMEOUT_MS 20000ms send deadline, added by #604 after this loop was found genuinely unbounded.
kernel/net/ftp.c|SLEEP_POLL|7699bf706a49  # kernel/net/ftp.c:170 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (FTP client has no callers). 20000ms no-progress deadline reset on each byte, plus the pos < max_len-1 byte cap.
kernel/net/ftp.c|SLEEP_POLL|e21e2c4d6372  # kernel/net/ftp.c:486 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (FTP client has no callers). Fixed 1000ms drain window on sched_now_ms(), proc_sleep(1) per iteration.
kernel/net/ftp.c|SLEEP_POLL|c5bebca4e25a  # kernel/net/ftp.c:683 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (FTP client has no callers). FTP_DATA_TIMEOUT_MS 30000ms no-progress deadline, in both ftp_ls and the identical loop in ftp_get.
kernel/net/ftp.c|SLEEP_POLL|c5bebca4e25a  # kernel/net/ftp.c:796 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (FTP client has no callers). FTP_DATA_TIMEOUT_MS 30000ms no-progress deadline, in both ftp_ls and the identical loop in ftp_get.
kernel/net/ftp.c|SLEEP_POLL|3ebe9bcf8328  # kernel/net/ftp.c:893 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE (FTP client has no callers). FTP_DATA_TIMEOUT_MS 30000ms no-progress deadline on the STOR data send.
kernel/net/http2.c|SLEEP_POLL|a5d0a21a3bab  # kernel/net/http2.c:466 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 20000ms wall-clock no-progress deadline re-armed on every byte, proc_sleep(2) per poll. Waiting on a remote peer, where a timeout is correct semantics per the CLAUDE.md rule.
kernel/net/http2.c|SLEEP_POLL|cf6df1108870  # kernel/net/http2.c:507 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Hard cap of 5000 consecutive WOULD_BLOCK spins x proc_sleep(2), about 10s, then -1. Remote-peer wait, timeout is correct semantics.
kernel/net/https.c|SLEEP_POLL|b757ece8e7a6  # kernel/net/https.c:47 SLEEP_POLL [DEBT] REVIEWED #287pass 2026-08-03. HARMFUL. https_tcp_send has NO deadline of any kind: on TCP_ERR_WOULD_BLOCK it does net_poll/tcp_timer/proc_sleep(1) and retries forever. tcp_send returns WOULD_BLOCK whenever in_flight >= snd_wnd, and with snd_wnd==0 and nothing unacked the retransmit timer never fires (it needs snd_una < snd_nxt) and there is no persist/zero-window probe in tcp.c, so a peer advertising a zero window wedges this loop permanently in the caller: a Ring-3 app blocked in sys_http_fetch, or the kernel desktop thread via the kernel-browser fallback. Every sibling send loop in the tree has a deadline; this is an omission, not a design choice.
kernel/net/https.c|SLEEP_POLL|6144816d5fd4  # kernel/net/https.c:76 SLEEP_POLL [DEBT] net_poll()-driven TLS receive wait on a remote peer. Correct semantics is a timeout; the poll should route through wait_event_timeout once TCP arms a wake (#426).
kernel/net/https.c|SLEEP_POLL|8e27225f528f  # kernel/net/https.c:146 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 4000ms ARP warm-up window, proc_sleep(2) per poll.
kernel/net/https.c|SLEEP_POLL|7f1494374b7b  # kernel/net/https.c:201 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 2 attempts x (3000ms connect wait + 200ms backoff), about 6.4s worst case, on a remote-peer connect where a timeout is correct semantics.
kernel/net/https.c|SLEEP_POLL|a0066ad872f8  # kernel/net/https.c:222 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 3000ms per-attempt timeout plus an early break on TCP_STATE_CLOSED, proc_sleep(2) per poll.
kernel/net/https.c|SLEEP_POLL|1f3334d63057  # kernel/net/https.c:1063 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 30000ms whole-fetch deadline on the https_get side; the second recorded line is a different function (https_post), bounded by 4000 would-block iterations x proc_sleep(5), about 20s.
kernel/net/https.c|SLEEP_POLL|1f3334d63057  # kernel/net/https.c:1371 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 30000ms whole-fetch deadline on the https_get side; the second recorded line is a different function (https_post), bounded by 4000 would-block iterations x proc_sleep(5), about 20s.
kernel/net/ipp.c|SLEEP_POLL|dc4a54428f94  # kernel/net/ipp.c:149 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 4000ms ARP warm-up window, proc_sleep(2) per poll.
kernel/net/ipp.c|SLEEP_POLL|cf6df1108870  # kernel/net/ipp.c:167 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 10000ms no-progress deadline with start re-armed on every byte sent, on a remote printer where a timeout is correct semantics.
kernel/net/ipp.c|SLEEP_POLL|433ecacae386  # kernel/net/ipp.c:197 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Bounded but LARGE: 3 attempts x (5000ms connect + two 10000ms sends + 20000ms recv + 300ms backoff), about 135s absolute worst case, and it is reachable from sys_print_text/_image/_screen, so printing to a dead printer freezes the calling app's window for over two minutes. Correct timeout semantics for a remote peer; the bound itself deserves shrinking.
kernel/net/ipp.c|SLEEP_POLL|18f181d3d88e  # kernel/net/ipp.c:213 SLEEP_POLL [DEBT] IPP printer TCP handshake wait on a remote peer. Timeout semantics are right; the proc_sleep(2) poll is the part that should become wait_event_timeout (#426).
kernel/net/ipp.c|SLEEP_POLL|03767127cd64  # kernel/net/ipp.c:250 SLEEP_POLL [DEBT] IPP response read-to-EOF wait on a remote peer. Same shape as ipp.c:213: correct timeout, wrong wait primitive (#426).
kernel/net/nfs.c|SLEEP_POLL|19af7c3ed938  # kernel/net/nfs.c:1862 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 100 iterations x proc_sleep(1), about 100ms unconditional warm-up, in the smbtest worker process, never on a UI thread.
kernel/net/nfs.c|SLEEP_POLL|e8656e966b6a  # kernel/net/nfs.c:1977 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 200 iterations x proc_sleep(1), about 200ms; self-test only, gated on /CONFIG/NFSTEST.CFG, in the smbtest worker.
kernel/net/rpc.c|SLEEP_POLL|362af8a4d2dc  # kernel/net/rpc.c:662 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 5000ms ARP pre-resolve window; rpc_net_pump is net_poll + tcp_timer + proc_sleep(1), so it yields every pass.
kernel/net/rpc.c|SLEEP_POLL|d28762f856d6  # kernel/net/rpc.c:668 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 4 connect attempts, each capped at 6000ms plus two about 50ms backoffs, about 24s worst case on a remote peer.
kernel/net/rpc.c|SLEEP_POLL|56ce57894047  # kernel/net/rpc.c:678 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed for (k = 0; k < 50) rpc_net_pump backoff, about 50ms, not a condition wait.
kernel/net/rpc.c|SLEEP_POLL|3eaac9c834e3  # kernel/net/rpc.c:683 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 6000ms per-attempt connect timeout plus an early break on TCP_STATE_CLOSED.
kernel/net/rpc.c|SLEEP_POLL|56ce57894047  # kernel/net/rpc.c:694 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed for (k = 0; k < 50) rpc_net_pump backoff, about 50ms, not a condition wait.
kernel/net/rpc.c|SLEEP_POLL|5e93602d7838  # kernel/net/rpc.c:716 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 10000ms no-progress send deadline with sstart re-armed on every accepted chunk.
kernel/net/rpc.c|SLEEP_POLL|c5bebca4e25a  # kernel/net/rpc.c:730 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. No-progress recv deadline of caller-supplied timeout_ms (nfs.c passes 5000 or 10000); the while (1) has a timeout return on every path.
kernel/net/rpc.c|SLEEP_POLL|2a9855b8008b  # kernel/net/rpc.c:784 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. UDP response deadline of caller-supplied timeout_ms, 5000 or 10000ms.
kernel/net/rtl8139.c|DELAY_SPIN|ed9ede29970e  # kernel/net/rtl8139.c:110 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. DEAD CODE: nothing outside net/rtl8139.[ch] references rtl8139_init; net.c probes virtio-net and e1000 only. Bounded anyway at timeout=1000 x io_wait(), about 1ms, then returns -1.
kernel/net/smb.c|SLEEP_POLL|cf6df1108870  # kernel/net/smb.c:172 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 10000ms no-progress deadline on the real sched_now_ms() clock, yielding via smb_net_pump -> proc_sleep(1). Reachable from the SMB VFS path, so it blocks only the calling app's thread.
kernel/net/smb.c|SLEEP_POLL|559c41477d31  # kernel/net/smb.c:221 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Caller-supplied budget_ms deadline on sched_now_ms(), reset on data, yielding 1ms per iteration.
kernel/net/smb.c|SLEEP_POLL|59f3dc36ef18  # kernel/net/smb.c:246 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Same budget_ms deadline as the header loop, chunked at 32000 bytes, yielding 1ms per iteration.
kernel/net/smb.c|SLEEP_POLL|cb5f35638317  # kernel/net/smb.c:1299 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Hard 5000ms ARP warm-up break on sched_now_ms(), yielding 1ms per iteration; worst case 5s on the mounting app's thread.
kernel/net/smb.c|SLEEP_POLL|d28762f856d6  # kernel/net/smb.c:1313 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Connect retry capped at 4 attempts, each bounded by the 6000ms connect timeout plus about 50ms settle, so about 24s worst case on the mounting app's thread.
kernel/net/smb.c|SLEEP_POLL|56ce57894047  # kernel/net/smb.c:1326 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed for (k = 0; k < 50) smb_net_pump, exactly 50 x proc_sleep(1), about 50ms of yielding, not a busy spin.
kernel/net/smb.c|SLEEP_POLL|a0066ad872f8  # kernel/net/smb.c:1332 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 6000ms connect timeout on sched_now_ms() plus an early break on TCP_STATE_CLOSED, yielding 1ms per iteration.
kernel/net/smb.c|SLEEP_POLL|56ce57894047  # kernel/net/smb.c:1343 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed for (k = 0; k < 50) smb_net_pump, exactly 50 x proc_sleep(1), about 50ms of yielding, not a busy spin.
kernel/net/smb.c|SLEEP_POLL|19af7c3ed938  # kernel/net/smb.c:1921 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 100-iteration ARP/stack warm-up, each iteration proc_sleep(1), about 100ms bounded yield.
kernel/net/smb.c|SLEEP_POLL|19af7c3ed938  # kernel/net/smb.c:1953 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 100-iteration ARP/stack warm-up, each iteration proc_sleep(1), about 100ms bounded yield.
kernel/net/smb.c|SLEEP_POLL|19af7c3ed938  # kernel/net/smb.c:2208 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 100-iteration ARP/stack warm-up, each iteration proc_sleep(1), about 100ms bounded yield.
kernel/net/smb.c|SLEEP_POLL|e8656e966b6a  # kernel/net/smb.c:2297 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Fixed 200-iteration warm-up, about 200ms of yielding, inside smb_run_selftest, which returns immediately unless /CONFIG/SMBTEST.CFG exists and otherwise runs in its own low-priority smbtest worker proc.
kernel/net/ssh/ssh2.c|SLEEP_POLL|cf6df1108870  # kernel/net/ssh/ssh2.c:167 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 10000ms no-progress send deadline on sched_now_ms(), proc_sleep(1) on WOULD_BLOCK. NOTE: this is one of three near-verbatim copies of the same send-all loop (smb.c, ssh2.c, ssh2_server.c); one shared helper would be the right cleanup.
kernel/net/ssh/ssh2.c|SLEEP_POLL|c837787f6655  # kernel/net/ssh/ssh2.c:296 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. All three loops carry a real remote-peer cap: ssh2_expect 12000ms, ssh2_read_version 10000ms, password-auth 12000ms, each iteration proc_sleep(2).
kernel/net/ssh/ssh2.c|SLEEP_POLL|c837787f6655  # kernel/net/ssh/ssh2.c:334 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. All three loops carry a real remote-peer cap: ssh2_expect 12000ms, ssh2_read_version 10000ms, password-auth 12000ms, each iteration proc_sleep(2).
kernel/net/ssh/ssh2.c|SLEEP_POLL|22b196b3a3c0  # kernel/net/ssh/ssh2.c:404 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 10000ms TCP connect deadline on sched_now_ms() plus an early bail on TCP_STATE_CLOSED, proc_sleep(2) per iteration.
kernel/net/ssh/ssh2.c|SLEEP_POLL|c837787f6655  # kernel/net/ssh/ssh2.c:629 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. All three loops carry a real remote-peer cap: ssh2_expect 12000ms, ssh2_read_version 10000ms, password-auth 12000ms, each iteration proc_sleep(2).
kernel/net/ssh/ssh2.c|SLEEP_POLL|b2eb2caab308  # kernel/net/ssh/ssh2.c:866 SLEEP_POLL [DEBT] REVIEWED #287pass 2026-08-03. HARMFUL. while (!cli->closed) has NO timeout and no TCP-state check; cli->closed is set only by an explicit MSG_CHANNEL_CLOSE/MSG_DISCONNECT or a framing error, and ssh2_pump DISCARDS ssh2_ingest()'s negative return, so an RST, half-open or silently-dropped peer leaves this loop spinning at about 200 wakeups/sec forever inside the SYS ssh syscall and the terminal app never returns. Its own server-side counterpart already breaks on TCP_STATE_CLOSED/CLOSE_WAIT/LAST_ACK; the client copy just lacks it.
kernel/net/ssh/ssh2_server.c|SLEEP_POLL|cf6df1108870  # kernel/net/ssh/ssh2_server.c:142 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Server copy of sock_send_all, identical 10000ms no-progress cap, proc_sleep(1) on WOULD_BLOCK.
kernel/net/ssh/ssh2_server.c|SLEEP_POLL|c837787f6655  # kernel/net/ssh/ssh2_server.c:212 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. srv_expect 15000ms, srv_read_version 10000ms, and a session loop that breaks on s->closed or TCP CLOSED/CLOSE_WAIT/LAST_ACK with a 60000ms no-shell-started cap; all yield proc_sleep(2), in the sshd worker proc, never the UI thread.
kernel/net/ssh/ssh2_server.c|SLEEP_POLL|c837787f6655  # kernel/net/ssh/ssh2_server.c:231 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. srv_expect 15000ms, srv_read_version 10000ms, and a session loop that breaks on s->closed or TCP CLOSED/CLOSE_WAIT/LAST_ACK with a 60000ms no-shell-started cap; all yield proc_sleep(2), in the sshd worker proc, never the UI thread.
kernel/net/ssh/ssh2_server.c|SLEEP_POLL|c837787f6655  # kernel/net/ssh/ssh2_server.c:582 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. srv_expect 15000ms, srv_read_version 10000ms, and a session loop that breaks on s->closed or TCP CLOSED/CLOSE_WAIT/LAST_ACK with a 60000ms no-shell-started cap; all yield proc_sleep(2), in the sshd worker proc, never the UI thread.
kernel/net/tls/tls.c|SLEEP_POLL|2d8edf7cca8d  # kernel/net/tls/tls.c:271 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Only polls once a record header is PARTIALLY read (it returns WOULD_BLOCK at a clean boundary), and is bounded by tls_deadline_expired(), always armed on both entry paths: tls_connect 20s and tls_recv 30s on the shared mono_ms() clock.
kernel/net/tls/tls.c|SLEEP_POLL|0af8338b4e43  # kernel/net/tls/tls.c:302 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. Record-body loop under the same always-armed 20s/30s real-time deadline, proc_sleep(2) per iteration.
kernel/net/tls/tls.c|SPIN_BUSYWAIT|7824de3bf524  # kernel/net/tls/tls.c:2811 SPIN_BUSYWAIT [LEGIT] REVIEWED #287pass 2026-08-03. tls_parse_rust_selftest, called once from main.c during boot: 200 warm-up plus 2000 timed iterations, and each inner walk terminates after exactly 3 handshake messages in a fixed 24-byte buffer.
kernel/net/tls/tls.c|SPIN_BUSYWAIT|7824de3bf524  # kernel/net/tls/tls.c:2813 SPIN_BUSYWAIT [LEGIT] REVIEWED #287pass 2026-08-03. tls_parse_rust_selftest, called once from main.c during boot: 200 warm-up plus 2000 timed iterations, and each inner walk terminates after exactly 3 handshake messages in a fixed 24-byte buffer.
kernel/net/tls/tls.c|SPIN_BUSYWAIT|96acb552d800  # kernel/net/tls/tls.c:2815 SPIN_BUSYWAIT [LEGIT] REVIEWED #287pass 2026-08-03. The Rust arm of the same boot-time measurement: 2000 iterations x a 3-message walk over a fixed buffer.
kernel/net/virtio_net.c|DELAY_SPIN|82d6ee80572c  # kernel/net/virtio_net.c:235 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. Post-reset settle of exactly 1000 io_wait() calls, about 1ms; virtio_net_init() is called only from net_init during boot.
kernel/net/wget.c|SLEEP_POLL|e853b3932ab2  # kernel/net/wget.c:149 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. 16000ms connect timeout on sched_now_ms() (the #499 fix) plus an early bail on TCP_STATE_CLOSED, and an explicit proc_sleep(2) so the compositor keeps running.
kernel/net/wget.c|SLEEP_POLL|4324d2cbd109  # kernel/net/wget.c:1108 SLEEP_POLL [LEGIT] REVIEWED #287pass 2026-08-03. TWO real caps: a caller-supplied idle_timeout_ms reset on every byte AND an absolute 90000ms overall deadline. It sleeps only when the socket is dry with nothing in hand, so it is a drain loop rather than a busy-wait. This is the shape the SMB and SSH send/recv loops should adopt.
kernel/proc/syscall.c|SLEEP_POLL|ca71a9ac0109  # kernel/proc/syscall.c:798 SLEEP_POLL [DEBT] REVIEWED #426ph2. sys_ping: two proc_sleep(20)/proc_sleep(5) poll loops waiting for an ICMP echo reply. Wake source is a REMOTE peer so a bound is correct semantics, but the shape is wrong: it should be an icmp-reply wait queue woken from the RX path plus wait_event_timeout, not a hand-rolled poll. Low blast radius (user-invoked ping only), so ranked below the I/O and IPC sites.
kernel/proc/users.c|DELAY_SPIN|d7e57c76ee2b  # kernel/proc/users.c:274 DELAY_SPIN [LEGIT] REVIEWED #287pass 2026-08-03. max_attempts is 3, so the backoff runs at most twice (about 0.9s total) and only on a FAILED read; documented as running before sti(), and its other caller is the kernel login screen, still pre-desktop.
kernel/serial.c|SPIN_BUSYWAIT|402bf82c76d3  # kernel/serial.c:59 SPIN_BUSYWAIT [DEBT] REVIEWED #713 2026-08-06. serial_read() is a genuinely UNBOUNDED while (!serial_received(port)) and it now has ZERO callers anywhere in the kernel: its only callers were dos_getchar() and the int21.c handlers, and dos/dos.c + dos/int21.c were deleted in #713 (measured with nm against every linked object plus librustkern.a). The historical serial FREEZE was the other function, serial_write THRE spin, hard-capped at 200000 iterations and dropping the character rather than hanging. Cap or delete serial_read before anyone gives it a caller.
userland/apps/arena/arena_pthread.c|DELAY_SPIN|894a1e925e0e  # userland/apps/arena/arena_pthread.c:838 DELAY_SPIN [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/chess/chess_pthread.c|DELAY_SPIN|894a1e925e0e  # userland/apps/chess/chess_pthread.c:838 DELAY_SPIN [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/chess/main.c|SPIN_BUSYWAIT|cb74ca3b1ddf  # userland/apps/chess/main.c:211 SPIN_BUSYWAIT [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/hdadbg/main.c|SPIN_BUSYWAIT|c37683cd9e77  # userland/apps/hdadbg/main.c:32 SPIN_BUSYWAIT [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/hdadbg/main.c|SLEEP_POLL|c2639e2d7dcd  # userland/apps/hdadbg/main.c:116 SLEEP_POLL [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/ide/ide_pthread.c|DELAY_SPIN|894a1e925e0e  # userland/apps/ide/ide_pthread.c:838 DELAY_SPIN [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/otaupd/main.c|SPIN_BUSYWAIT|15587cb99aa4  # userland/apps/otaupd/main.c:391 SPIN_BUSYWAIT [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/reloctest/main.c|SPIN_BUSYWAIT|c837787f6655  # userland/apps/reloctest/main.c:46 SPIN_BUSYWAIT [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/apps/spindisk/main.c|SPIN_BUSYWAIT|a4154585810d  # userland/apps/spindisk/main.c:15 SPIN_BUSYWAIT [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/libc/pthread.c|DELAY_SPIN|894a1e925e0e  # userland/libc/pthread.c:838 DELAY_SPIN [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
userland/test/hello.c|SPIN_BUSYWAIT|965df2723e7e  # userland/test/hello.c:38 SPIN_BUSYWAIT [LEGACY] Userland app wait loop (not on the kernel build gate path; scanned only by `make lint-all`). Bulk-baselined at #514 when the lint's == blindness was fixed; NOT individually reviewed.
kernel/proc/process.c|YIELD_SPIN|c5bebca4e25a  # kernel/proc/process.c:1283 YIELD_SPIN [LEGIT] SURFACED BY #746, which widened YIELD_SPIN from proc_yield() to sched_schedule() as well. This is proc_wait_legacy_spin(), a DELIBERATE, FAITHFUL RE-CREATION of the pre-#230 scan+sched_schedule() spin that #230 deleted, kept as the control arm of the #230 A/B benchmark. It is compiled ONLY under -DPROCWAIT_BENCH (make PROCWAITBENCH=1), never in a normal or golden build, and has no callers outside the benchmark. Converting it to wait_event() would destroy the thing it exists to measure. The lint finding it is the rule WORKING: it is exactly the shape #746 taught it to see.
kernel/gui/installer.c|SLEEP_POLL|863088c44390  # kernel/gui/installer.c:1043 SLEEP_POLL [LEGIT] Cooperative yield inside the disk-installer BULK COPY loop, fired once per ~2MB alongside a progress update. It is not a poll: nothing is being waited ON, so there is no condition a wait-queue could sleep against, and wait_event() has nothing to arm. Its purpose is to bound how long a multi-hundred-megabyte partition clone monopolises the CPU between progress callbacks, so the desktop and network stay responsive while installing. Removing it does not deadlock; it makes the machine unresponsive for the duration of the clone. Introduced with the #306 two-partition clone fix.
kernel/proc/process.c|YIELD_SPIN|b4fe489d5399  # kernel/proc/process.c sched_ap_enter() AP idle loop [LEGIT] REVIEWED #67 2026-08-12. This IS a core's idle loop, the AP counterpart of idle_process(); it is not a waiter and there is nothing for it to wait ON, so a wait_event here would be a waiter with no waker. The loop body ends in an UNCONDITIONAL sti+hlt, so the core halts and burns no host CPU: a halt loop, not a spin. It calls sched_schedule() rather than being interrupted into it because the periodic tick is the legacy PIT on IRQ0, delivered to the BSP only, so an AP that merely halted would never enter the scheduler at all (MEASURED build 248: cpu1 held a 4-deep run queue with 0 context switches for a whole AssaultCube burn).
kernel/cpu/smp.c|SPIN_BUSYWAIT|f1c294600039  # kernel/cpu/smp.c bkl_probe_selftest() calibration delay [LEGIT] REVIEWED #67 2026-08-12. A DELIBERATE, one-shot, clock-bounded 5 ms busy-wait whose entire purpose is to hold the BKL for a KNOWN duration so the hold-time probe can be checked against it. There is nothing to wait ON - the wait IS the measurement - so wait_event() has no meaning here. Runs once, at boot, on the BSP, before any AP schedules and before the desktop exists, and is bounded by mono_us() rather than an iteration count. It exists because three instruments in this ticket were believed and were wrong (a spin counter that was 93% of its own reading, an acquire counter with the same shared-cacheline defect, and a hold timer that could not say which core it timed).
