# Kernel Rust-port ledger (for the end-of-run code / perf / security summary)

Each ported component appends a row. Perf = RDTSC cycles/op of _rs vs _c over the boot self-test corpus (measure for pure/hot functions; "parity" is an honest, common result under the kernel's soft-float/-mno-sse build). Security = the C bug class Rust removes BY CONSTRUCTION (slice bounds-checks + no unchecked pointer arithmetic), especially for parsers of untrusted (disk/wire/file) input.

| component | build | C LOC repl | Rust LOC | differential | perf (C -> RS cyc/op, ratio) | security class removed | notes |
|---|---|---|---|---|---|---|---|
| net/ip.c ip_checksum | b792 | ~18 | ~22 | 207,507 + 5,597, 0 mism | ~parity (PoC; soft-float) | none material (fixed-size bounded); removes a technically-unaligned u16 read | LIVE |
| fs/ext2.c ext2_dirblock_find | b794 | ~40 | ~55 | 516k(PoC)+38,373(live, incl malformed), 0 mism | 2178 -> 150 (14x) - HONEST: memcpy-idiom removal under -fno-builtin, NOT language | REMOVES OOB-read class: 3 unchecked disk-controlled accesses (== the live #476 bug class), removed by construction | LIVE (ext2-root); the flagship security win so far |
| net/tcp.c tcp_checksum | b795 | ~25 | ~28 | 5,603, 0 mism | ~parity | low surface | LIVE |
| net/udp.c udp_checksum | b795 | ~20 | ~24 | 5,603, 0 mism | ~parity | low surface | STAGED (not on wire) |
| crypto/sha256.c sha256_transform | b796 | ~40 | ~52 | 20,000 + 3 NIST KAT, 0 mism | parity est (not micro-benched this run; 32-bit soft-float hash) | low surface (fixed-size); removes unchecked ptr arithmetic in the schedule | LIVE; verified VM2971 |
| crypto/sha512.c sha512_transform | b797 | ~50 | ~64 | 20,000 + 3 NIST KAT, 0 mism | C=1091 -> RS=1038 cyc/op, ratio=0.95 (RDTSC, ~parity; RS marginally faster; 64-bit soft-ALU hash) | low surface (fixed-size); removes unchecked ptr arithmetic in the 80-word schedule | LIVE; verified throwaway VM2988 (destroyed), golden untouched |
| crypto/md5.c md5_transform | b798 | ~97 | ~78 | 20,000 + 3 RFC1321 KAT (live) + 500k (local pre-flight), 0 mism | C=301 -> RS=494 cyc/op, ratio=1.64 (RDTSC; HONEST: RS ~64% SLOWER - my rolled+branchy 64-round loop vs the C's fully-unrolled 64 FF/GG/HH/II macros; MD5 is a COLD path - HMAC-MD5/NTLM in net/smb.c only) | low surface (fixed-size 64B block); removes unchecked ptr arithmetic in the LITTLE-endian message decode | LIVE; verified throwaway VM2989 (destroyed), golden untouched |
| crypto/md4.c md4_transform | b799 | ~80 | ~70 | 20,000 + 3 RFC1320 KAT (live) + 2,000,000 (local pre-flight), 0 mism | C=203 -> RS=160 cyc/op, ratio=0.78 (RDTSC; RS ~21% FASTER - unlike MD5 I UNROLLED the 48-step FF/GG/HH sequence exactly like the C's macros, so no rolled-loop penalty; MD4 is a COLD path - NTLM auth in net/smb.c only) | low surface (fixed-size 64B block); removes unchecked ptr arithmetic in the LITTLE-endian message decode | LIVE; verified throwaway VM2990 (destroyed), golden untouched |
| crypto/chacha20.c chacha20_block | b800 | ~26 | ~40 | 20,000 + 1 RFC 8439 2.3.2 KAT (live) = 20,001, 0 mism | C=374 -> RS=276 cyc/op, ratio=0.73 (RDTSC; RS ~27% FASTER - the 20-round quarter-round core rolled in a 10-iter double-round loop optimizes well; ChaCha20 backs TLS ChaCha20-Poly1305, a warm TLS path) | low surface (fixed-size 16-word in / 64-byte out block core); removes unchecked ptr arithmetic in the LITTLE-endian keystream serialization + working-state indexing | LIVE; verified throwaway VM2991 (destroyed), golden untouched; desktop "build 800" screenshot-confirmed |
| crypto/aes.c aes_encrypt_block + aes_decrypt_block | b801 | ~130 (2 block cores + Sub/Shift/Mix/AddRoundKey leaves + xtime/gf_mul + 2 S-boxes) | ~180 | 4 FIPS-197 KAT (AES-128 C.1 + AES-256 C.3, enc+dec via live API) + 40,000 (20,000 random (state,round-keys,Nr in{10,12,14}) x enc+dec) = 40,004, 0 mism | C=984 -> RS=592 cyc/op, ratio=0.60 (RDTSC; RS ~40% FASTER - plain S-box round form, forward path; the C is also plain S-box (NOT T-table), so this is a like-for-like win, not a table-vs-loop artifact; AES backs TLS AES-GCM in crypto/crypto.c + SSH aes-ctr in net/ssh/*, WARM paths) | low surface (fixed-size 16-byte block, trusted key schedule); removes unchecked ptr arithmetic in the round-key indexing (rk[4*round+i]) + column-major state accesses, bounds-checked by construction | LIVE; verified throwaway VM2992 (destroyed), golden untouched; desktop "v1.95.0 (build 801)" screenshot-confirmed; enc+dec both routed |
| crypto/{hmac,sha256,sha512}.c hmac_md5 + hmac_sha256 + hmac_sha384 (one-shot CONSTRUCTIONS) | b802 | ~24 (one shared RFC 2104 wrapper x 3 public one-shots; construction, NOT compression) | ~90 (shared hmac_construct + 3 thin _rs wrappers + extern hash decls) | 6 KAT (RFC 4231 HMAC-SHA256 TC1/TC2 + HMAC-SHA384 TC1/TC2 via live API + RFC 2202 HMAC-MD5 TC1/TC2) + 21,000 (7,000 random (key,msg) x 3 variants) hmac_*_rs vs INDEPENDENT hmac_*_c (incremental HMAC) = 21,006, 0 mism; +200k local pre-flight | C=5084 -> RS=5400 cyc/op, ratio=1.06 (RDTSC HMAC-SHA256; HONEST: RS ~6% SLOWER - HMAC is ~4 sha256 compressions already-Rust in both paths, the delta is pure FFI/wrapper overhead of building ipad/opad + crossing init/update/final vs the C one-shot's inlined path; HMAC-SHA256 is a WARM path: TLS 1.3 Finished key schedule + CSPRNG HMAC-DRBG) | low surface (construction over a trusted key + already-Rust hash); removes unchecked ptr arithmetic in the K'-pad + ipad/opad XOR (bounds-checked [u8;128] by construction); hash ctx crosses FFI as an OPAQUE 16-aligned bounded 256B buffer (no C struct duplicated, sizeof asserted <=256) | LIVE; construction CALLS BACK into the C hash API by design (compression stays Rust under -DRUST_SHA256/512/MD5); incremental hmac_*_init/update/final stay C this phase; verified throwaway VM2993 (destroyed, desktop "v1.95.0 (build 802)" screenshot-confirmed, [CSPRNG] selftest PASS exercises live HMAC-SHA256), golden untouched. COMPLETES the crypto tier. |
| net/arp.c arp_parse (incoming ARP frame parse/validate) | b804 | ~26 (incoming-parse portion of arp_handle refactored verbatim into arp_parse_c) | ~70 (arp_parse_rs + ArpParsed #[repr(C)], sizeof asserted ==28) | 512 (well-formed Ethernet/IPv4 ARP request/reply len 28..64 + truncated 0..27 + bad hw/proto type + bad hlen/plen, rs==c), 0 mism; +263,000 local pre-flight (138,000 well-formed + truncated + bad-field + fully-random, 0 mism) | C=124 -> RS=14 cyc/op, ratio=0.11 (RDTSC; RS faster, but HONEST: this is a micro-bench artifact - the tiny 28-byte parse lets the optimizer fold the Rust `out` writes/copy_from_slice while the C keeps its memcpy+ntohl/ntohs calls under -fno-builtin/soft-float; treat as "Rust at least as fast", not a real 9x. ARP is a warm-ish path: every LAN next-hop resolution) | **SECOND TIER-2 UNTRUSTED-WIRE PARSER, but HONEST latent-not-reachable.** Removes the UNCHECKED-WIRE-POINTER-ARITHMETIC class by construction (every field access is a bounds-checked index into a slice of exactly len bytes). UNLIKE icmp/ext2 there is NO reachable OOB in the C reference: arp_parse_c already (1) length-gates len>=sizeof(arp_header_t)==28 BEFORE any field read, (2) reads every address field at a FIXED offset inside those 28 bytes (hw_len/proto_len are validated ==6/4 but never used to compute an offset), and (3) is READ-ONLY (no in-place mutation, unlike icmp's checksum zero+restore). So [RUST-SEC] arp reports 32/32 malformed verdicts IDENTICAL, 0 divergences - the Rust is DEFENSE-IN-DEPTH (would confine an OOB if the fixed-offset assumption were ever broken by a struct change or a future variable-length HW/proto address keyed off hlen/plen), NOT a live-bug fix. | LIVE (arp_handle -> arp_parse -> tail-jmp arp_parse_rs proven by objdump); arp_parse_c kept for rollback. DAD/cache-add/reply-send stay in C. Verified throwaway VM2299 (destroyed): desktop "v1.95.0 (build 804)" 2 differing screendumps (screensaver anim + woken desktop) + idle RIP=0x58207a just past `sti;hlt` (HLT idle, no busy-spin) + LIVE ARP: the build server who-has <LAB_HOST> -> VM Reply is-at bc:24:11:5f:d9:99 (reply built through arp_parse_rs) + ping 3/3 + live TCP/HTTP to HA <HOME_ASSISTANT_HOST> (next-hop MAC resolved via an incoming ARP reply parsed by arp_parse_rs) + HA "Sun" widget populated. Golden + VMs 2200/2251/106/2160 untouched. |
| net/icmp.c icmp_parse (incoming ICMP packet parse/validate) | b803 | ~24 (incoming-parse portion of icmp_handle refactored verbatim into icmp_parse_c) | ~60 (icmp_parse_rs + IcmpParsed #[repr(C)] + icmp_ones_complement fold) | 2,994 (valid echo request/reply every len 8..1500 + too-short 0..7 + header-straddling + bad-checksum, rs==c), 0 mism; +2,994 local pre-flight (0 mism). **CORRECTED 2026-07-16: that PASS was TRUE AND WORTHLESS.** icmp_build_valid always writes `field = ~S`, so the ONLY state where the C's verdict (`~S == field`) and the shipped Rust's RFC-1071 form (`fold(S+field) == 0xFFFF`) disagree - ones-complement NEGATIVE ZERO, `S == 0xFFFF && field == 0xFFFF` - was ALGEBRAICALLY UNREACHABLE for the generator (measured: old builder reaches it 0/1493; new builder 1493/1493). The shipped Rust was therefore an undocumented EXPANSION (accept set a strict superset; the original sends 0 echo replies where it sent 1). FIXED 2026-07-16: icmp_parse_rs now computes the C's verdict exactly (icmp_ones_complement_zeroed), and icmp_build_negzero was added so the self-test can REACH the divergent state (measured: shipped Rust diverges on 1493/1493 of them, fixed Rust on 0/1493). The self-test now also reports negzero coverage next to its verdict, so a future blind spot shows as PASS(BLIND) rather than PASS | C=31 -> RS=32 cyc/op, ratio=1.03 (RDTSC; ~PARITY, RS ~3% slower - negligible on a tiny hot parser; ICMP echo is a cold/occasional path) | **FIRST TIER-2 UNTRUSTED-WIRE PARSER.** Removes 2 classes BY CONSTRUCTION: (1) MISSING UPPER BOUND on the attacker-controlled length - the verbatim C only checks len>=8, feeding len unchecked into ip_checksum(buf,len) AND a downstream `uint8_t reply[length]` KERNEL-STACK VLA (stack-overflow class); Rust rejects len>1500(MTU) before any deref. (2) IN-PLACE MUTATION of the const untrusted RX buffer during checksum verify (C zeroes+restores header->checksum, casting away const - a transient-corruption/aliasing hazard on a shared/DMA RX buffer); Rust folds over an IMMUTABLE slice, zero writes. [RUST-SEC] icmp: C accepts 49/49 oversize, Rust confined 49/49. **CORRECTED 2026-07-16 (#404 drift audit 2 + live-fix): the previous rating on this row was WRONG.** It read: "live the length is already MTU-bounded by ip.c's total_length<=frame guard + no IP reassembly, so both are DEFENSE-IN-DEPTH / latent-bug confinement, NOT a currently-reachable OOB". That reasoning was FALSE. ip_handle guarded `ihl <= length` and `total_length <= length` but NEVER `total_length >= ihl`, so `total_length - ihl` UNDERFLOWED and a crafted 60-byte frame (IHL=15, total_length=20) delivered **len=65496** to this parser. MEASURED by driving the real verbatim ip_handle: icmp_handle, udp_handle AND tcp_handle are each CALLED with len=65496 from a 60-byte frame; ASan witnesses the over-read in the real udp_handle (READ size 2, 4 bytes right of a 60-byte region) and the real tcp_handle prologue (READ size 1, 12 bytes right). So ICMP_MAX_LEN was confining a **REACHABLE, LAN-attacker-triggerable OOB** (unbounded checksum span + a ~64 KB `uint8_t reply[length]` VLA on a 16 KB kernel stack), and a -DRUST_ICMP ROLLBACK WAS NOT SAFE. Root cause fixed in plain C at the source (net/ip.c `total_length < ihl` guard, task #517 / MAYTERA-SEC-2026-0014); with that guard in place this bound is genuinely defense-in-depth again and rollback is safe. | LIVE (icmp_handle -> icmp_parse_rs direct-call proven by objdump); icmp_parse_c kept for rollback. Verified throwaway VM2350 (destroyed): desktop "v1.95.0 (build 803)" 2 differing screendumps + idle RIP=0x58177a CPL=0 HLT=1 + LIVE PING the build server->VM(<LAB_HOST>) 5/5 replies, 5 ECHO REQUESTs parsed by the live Rust; golden untouched. NOTE: a pre-existing INTERMITTENT login-spawn race (#264 spawn/CR3-IRQ, RIP=0x45 wild-jump in sched_schedule at "Starting login screen") fired on 1 of 3 boots of the IDENTICAL b803 kernel; NOT caused by this port (pure b802 backup + pristine b741 also boot; b803 with only the self-test CALL removed boots; the self-test's extra boot-time work merely perturbs timing). |

## End-of-run summary must report (user request):
- TOTAL code change: C LOC replaced vs Rust LOC added, # files/functions ported, # kernel components now Rust, % of a chosen denominator (e.g. crypto+net-parse LOC).
- PERFORMANCE: per-component C->Rust cyc/op + ratio (honest parity vs speedup, with the reason); aggregate note.
- SECURITY: enumerate bug classes removed by construction per component (OOB read/write, integer-overflow-into-index, unchecked-length), weighted to untrusted-input parsers; note any real bugs surfaced/fixed during porting.
| net/dns.c dns_parse_response (incoming DNS RESPONSE-message parse: QR/rcode/ancount + compression-pointer-aware name-skip + answer A-record walk) | b805 | ~55 (response-walk portion of dns_handle_response refactored verbatim into dns_parse_response_c, incl. dns_skip_name) | ~120 (dns_parse_response_rs + dns_skip_name_rs + DnsResult #[repr(C)], sizeof asserted ==16) | 512 boot (well-formed A / CNAME+A / AAAA-only / ancount=0 / rcode-err / not-a-response + malformed: short-header, truncated-mid-answer, compression-pointer LOOP, OOB pointer, oversized rdlength, label-past-end, random; rs==c) 0 mism; +3,000,000 offline pre-flight (all 13 kinds incl every malformed class, 922,784 well-formed A parses ip+ttl byte-identical) 0 mism; + C reference ASan+UBSan-clean over the same 3M (proves no OOB/UB) | C=30 -> RS=30 cyc/op, ratio=1.00 (RDTSC; PARITY - tiny bounded walk, both fold to ~the same; DNS resolve is a cold/occasional path) | **THIRD Tier-2 untrusted-wire parser (the on-paper flagship: DNS name-compression). HONEST latent-not-reachable, like arp - NOT a live bug.** Removes the unchecked-wire-pointer-arithmetic CLASS by construction (every label/pointer/record-field access is a bounds-checked index into a slice of exactly msglen bytes). The C reference is ALREADY memory-safe: dns_skip_name CANNOT loop or read past msglen (pos strictly increases by >=1 each iteration AND a compression pointer >=0xC0 returns IMMEDIATELY - it is never FOLLOWED), and every record-field read is length-gated (pos+4 / pos+10 / pos+rdlength <= length) before the access. So C and Rust reject/accept the ENTIRE attack corpus identically (812/812 boot, 0 divergence; 3M offline, 0 mism; ASan/UBSan clean) - there is NO reachable OOB/hang even on pointer loops/OOB pointers/oversized rdlength. The GENUINELY-STRONGER-THAN-ARP point: the C is safe ONLY because it never DECODES a name (never follows a pointer); the classic DNS pointer-LOOP / pointer-OOB CVE class becomes reachable the instant this parser is extended to decode names (CNAME/PTR target extraction, logging the answer owner name - the natural next feature). The Rust dns_skip_name_rs carries a strictly-decreasing visited budget so even a FUTURE pointer-follow could not loop; the raw-pointer C form could not bound it. Class-elimination today + pre-confinement of a class one feature away from reachable. | LIVE (dns_handle_response -> dns_parse_response -> tail-jmp dns_parse_response_rs proven by objdump: 482bf0 `jmp 5e4130 <dns_parse_response_rs>`); dns_parse_response_c kept for rollback + differential. UDP receive / transaction-id+QR match / cache-put / dns_query bookkeeping stay C. Verified throwaway VM2396 (destroyed): desktop "v1.95.0 (build 805)" 2 differing screendumps (Sun widget populated = live net) + idle RIP=0x582e4a in sched_schedule CPL=0 (HLT idle, no busy-spin) + LIVE DNS: the resolver parsed REAL upstream responses `[DNS] Resolved dns.google -> 8.8.4.4 (ttl=355s)` and `-> 8.8.8.8 (ttl=355s)` through dns_parse_response_rs. Golden + VMs 2200/2251/106/2160 untouched. |
| net/dhcp.c dhcp_parse (incoming DHCP reply parse: BOOTP header op/magic + option Type-Length-Value walk extracting msgtype53/subnet1/router3/dns6/lease51/serverid54/yiaddr) | b806 | ~50 (option-walk + op/magic checks of dhcp_handle/dhcp_parse_options refactored verbatim into dhcp_parse_c) | ~130 (dhcp_parse_rs + DhcpParsed #[repr(C)], sizeof asserted ==36) | 512 boot (well-formed OFFER/ACK/NAK + too-short + bad-op + bad-magic; rs==c) 0 mism; +3,000,000 offline pre-flight (2M structured differential incl 500,000 well-formed OK, 0 mism; 1M random-fuzz over-read characterization = 47,605 result-changing over-reads) + C reference ASan+UBSan-clean on the in-bounds corpus AND ASan-PROVEN heap-buffer-overflow READ in dhcp_parse_c on a tight-allocation 250-byte runt (Rust confined) | C=88 -> RS=60 cyc/op, ratio=0.68 (RDTSC; RS ~32% FASTER - the len-bounded slice walk optimizes better than the C's per-4-byte-option memcpy+ntohl under -fno-builtin/soft-float; DHCP is a cold boot + lease-renew path) | **FOURTH Tier-2 untrusted-wire parser and the FIRST with a genuinely REACHABLE over-read (stronger than arp/dns's latent cases; weaker than the ext2 #476 guaranteed heap crash).** The verbatim C option walk runs over the FIXED 308-byte options[] array driven ONLY by the attacker's option LENGTH bytes and NEVER consults `len` (dhcp_handle only gates the 240-byte BOOTP header), so a runt / non-END-terminated / lying-length OFFER (attacker-spoofable on the LAN) reads PAST the received bytes: opt[i+1] at i==307 is one past options[], and a 4-byte value is read after checking only olen>=4, NOT i+2+4<=308. [RUST-SEC] dhcp (live boot): C over-read past len on 400/400 crafted runt/lying-length OFFERs, Rust confined 400/400; offline 1M random-fuzz = 47,605 result-changing over-reads; ASan PROVES a real heap-buffer-overflow READ at the option-walk line in dhcp_parse_c on a tight 250-byte runt. HONEST severity: live `data` usually points into the >=1514-byte NIC RX buffer, so the over-read reads STALE/adjacent bytes (deriving netmask/gateway/dns/server-id from non-packet memory) rather than faulting - an info/logic hazard, not a guaranteed crash - but with a tight buffer it faults. The Rust CONFINES it by construction: immutable slice of exactly len bytes, option walk bounded to [240,len), every step bounds-checked so a length past the packet terminates the walk. | LIVE (dhcp_handle -> dhcp_parse -> tail-jmp dhcp_parse_rs proven by objdump: 481980 `jmp 5e4c30 <dhcp_parse_rs>`, and dhcp_handle inlines a direct `call 5e4c30 <dhcp_parse_rs>`); dhcp_parse_c kept for rollback + differential. Socket/state machine (xid match, DISCOVER/REQUEST send, DAD, bind) stays C. Verified throwaway VM2497 (destroyed): desktop "v1.95.0 (build 806)" (HA Sun widget populated = live net) + 2 differing screensaver frames + idle RIP=0x5839da CPL=0 HLT=1 (no busy-spin) + [RUST-DIFF] dhcp 512 0 mism PASS + [RUST-SEC] dhcp 400/400 over-read/confined + [RUST-PERF] dhcp C=88/RS=60 ratio=0.68. LIVE DHCP OFFER/ACK NOT exercised this boot (HONEST): the golden image has no static-IP file so it ran DHCP DORA, but no OFFER reached the VM (discovery timed out 3/3) and the net worker fell back to a self-assigned static IP (<LAB_HOST> after DAD); so dhcp_parse_rs was proven by the differential + offline + ASan, NOT a live server reply. Observed pre-existing (unrelated) `[HEAP] kfree invalid pointer` warnings from the b738 net-worker fallback path (this seam does ZERO heap ops). Golden + VMs 2200/2251/106/2160 untouched. **CORRECTION 2026-07-16 (3-way drift audit + FIX):** the b806 claim "the Rust is a pure confinement of the C" was **FALSE on the live path** and this row previously repeated it. The shipped Rust had TWO undocumented semantic divergences from the C, both needing len>548 and therefore INVISIBLE to the boot self-test (whose builder emitted only ~250-290 byte packets) and to any rs-vs-c differential: **(A) walk-window WIDENING** - the C walks a FIXED 308-byte options[] window ([240,548)) regardless of len, but the Rust walked [240,len), so for len>548 it HONORED options the C ignores, i.e. it read MORE, the OPPOSITE of a confinement; **(B) lying-length strictness** - the Rust rejected an option AND aborted the walk on the DECLARED olen even when the 4 bytes it reads were present, where the C extracts them. Measured **264,738 non-excusable divergences/1M** (A=176,487, B=88,251) under a 3-way oracle with a len-determinism classifier. FIXED: walk bounded to [240,min(len,548)) and every read gated on bytes ACTUALLY read (`vstart+4<=n`) with no early break -> **264,738 -> 0 over 3M vectors (3 seeds x 1M)**, ORIGINAL==TWIN still 0/1M, all 134,826/1M advisory-0002 confinements preserved. Self-test generator EXTENDED with a >548-byte corpus (`dhcp_build_long`, 3 shapes: window-widening PoC / lying-length-with-value-present / decoy-options-past-548); proven to catch the bug: the new corpus FAILS 256/256 against the old seam and PASSES 256/256 against the fixed one, while the old corpus passes against BOTH (max len 290). Rollback fidelity: INTACT (twin is a clean lift, 0/5M). NOT BUILT/BOOTED (isolated parallel-dev). |
| net/url.c url_parse (URL-string parse: scheme/userinfo/host/port/path/query/fragment split of a NUL-terminated URL) | b807 | ~150 (whole url_parse; already pure - no DNS/connect) | ~230 (url_parse_rs + UrlParsed #[repr(C)] sizeof asserted ==2194 + helpers: streq/default_port/scopy/strlen_from + char-class) | 512 boot (curated valid http/https/ftp/ws/wss/file/mailto/tel + IPv6 literals + reject: empty/ws-only/over-long fields/huge-port/unterminated-IPv6 + random http + fuzz; rs==c) 0 mism; +1,000,000 offline pre-flight (724,847 accepts incl all field combos + IPv6 + over-long-field + embedded-NUL + non-ASCII + `://`-only + structural fuzz) 0 mism (identical accept/reject AND identical parsed fields) + C reference ASan+UBSan-CLEAN over 300k NUL-terminated vectors + ASan-PROVEN 1-byte over-read in url_parse_c on a NON-terminated 64-byte heap buffer (Rust confined via URL_MAX_INPUT=8192) | C=391 -> RS=549 cyc/op, ratio=1.40 (RDTSC; RS ~40% SLOWER - HONEST: a branchy pointer-walk parser vs tuned C; URL parse is a COLD navigation/redirect path, so it does not matter; the value is memory-safety-by-construction) | **FIFTH Tier-2 untrusted-input parser (input = URL STRING from redirect Location headers + address bar). HONEST latent DEFENSE-IN-DEPTH, like arp/dns - NOT a reachable overflow.** UNLIKE dhcp/ext2 the C reference ALREADY CAPS every out-field copy (explicit `>= sizeof(field)` reject before scheme/userinfo/host/path/query/fragment + safe_copy on the mailto path), so a long host/path does NOT overflow the fixed url_t buffers (offline ASan+UBSan over 300k NUL-terminated vectors is CLEAN). The Rust removes the write class BY CONSTRUCTION (every out write is a bounds-checked slice index) AND caps the input scan at URL_MAX_INPUT=8192 - the C's ONE genuine gap: its `while(*p...)` scanners trust NUL-termination with NO upper bound (ASan proves a 1-byte over-read off a non-terminated buffer). Live callers (gui/browser.c) pass a NUL-terminated fixed char buffer, so the over-read is latent, not reachable. [RUST-SEC] url (boot): C caps hold, 300/300 over-long fields rejected identically by C and Rust (0 divergences); Rust bounds the input scan at 8192 (C unbounded). | LIVE (url_parse -> `call 5e8080 <url_parse_rs>` + setne, proven by objdump; url_parse_c 4a9ae0 retained for rollback + differential). url_resolve_relative/url_to_string/url_encode/url_decode stay C (url_resolve_relative calls url_parse so it gets the Rust parse free). Verified throwaway VM2807 (destroyed): desktop "v1.95.0 (build 807)" + HA "Sun/below_horizon" widget populated + 2 differing animated screensaver frames + woken desktop + idle RIP=0x58486a CPL=0 HLT=1 (no busy-spin) + [RUST-DIFF] url 512 0 mism PASS + [RUST-SEC] url 300/300 rejected identically + [RUST-PERF] url C=391/RS=549 ratio=1.40. LIVE PROOF (stronger than dhcp): serial `[HTTP] GET http://<HOME_ASSISTANT_HOST>:8123/api/states/sensor...` -> `[HTTP] Status: 200 OK` loop - each URL (host + port 8123 + multi-segment path) split by url_parse_rs before connect; the 200s + populated Sun widget prove correct scheme/host/port/path on live traffic. Golden + VMs 2200/2251/106/2160 untouched. |
| exec/elf.c elf_validate (ELF64 pre-map header + program-header validation: e_ident magic/class/data/version + e_type + e_machine(x86-64) + phdr-table bounds + PT_LOAD file-bounds walk) | b808 | ~80 (elf_validate ehdr/phdr-table checks + calculate_load_bounds PT_LOAD file-bounds walk, refactored verbatim into elf_validate_full_c) | ~230 (elf_validate_full_rs + ElfValidated #[repr(C)] sizeof asserted ==40 + bounds-checked LE readers) | 256 boot (well-formed synthetic ELF64 images + shared ehdr/phdr-table reject mutations: bad magic/class/data/machine/type/version, phnum=0, phoff past EOF, phdr-table past EOF, truncated header, zero filelen; rs==c) 0 mism; +2,000,000 offline pre-flight (well-formed + all reject classes, 0 mism) over 7 REAL /apps ELFs (cat/pwd/ls/echo/terminal/files/settings all ACCEPT with byte-identical fields) + ASan/UBSan on the C ref | C=35 -> RS=154 cyc/op, ratio=4.40 (RDTSC; HONEST: RS ~4.4x SLOWER - the C is a trivial inlined field-read walk that -O2 folds to ~35 cyc; the Rust crosses FFI + bounds-checks every read; ELF validation is a COLD load-time path, once per app launch, so ~120 extra cycles is negligible; the value here is memory-safety, not speed) | **SIXTH Tier-2 untrusted-input parser and the STRONGEST security win of the series: TWO ASan-PROVEN REACHABLE OOBs in the C reference, one a read+WRITE (not just a read like dhcp/ext2).** Removes THREE classes by construction (slice of exactly filelen bytes, every field read + phdr access bounds-checked, correct no-underflow bounds math): (1) OVERSIZED p_filesz - calculate_load_bounds gates each PT_LOAD with check_overflow_add(p_offset,p_filesz,size)==(p_offset > size-p_filesz), which UNDERFLOWS when p_filesz>size, so an oversized p_filesz PASSES validation and elf_load's memcpy(dest, elf_data+p_offset, p_filesz) (elf.c:771) OOB-READS the file heap buffer AND OOB-WRITES past the memsz-sized load allocation (ASan-proven heap-buffer-overflow READ of 4216 bytes; reachable on any crafted ELF); (2) UNDERSIZED e_phentsize - elf_validate bounds the phdr TABLE end with the attacker's e_phentsize but never checks e_phentsize>=sizeof(Elf64_Phdr)=56, so every elf_get_phdr's 56-byte read over-reads for a small phentsize (ASan-proven 8-byte over-read); (3) p_memsz<p_filesz - segment copy writes filesz into a memsz-sized region (latent write-overflow). [RUST-SEC] elf (live boot): C accepted+RS confined 100/100 for each of the three classes (300/300 total); offline 1M-vector characterization ~333k each 100% confined. HONEST: (1) and (2) are REACHABLE in the live loader today; the live -DRUST_ELF path CONFINES them (elf_load bails on the Rust reject before calculate_load_bounds/the copy run) - so this is a live security IMPROVEMENT, not just latent. Also a candidate plain-C fix (fix check_overflow_add's underflow + add the phentsize/memsz gates), like the ext2 #476 find; flagged in blame.md. | LIVE (elf_validate -> 0x53754c `call 5e6a90 <elf_validate_full_rs>` proven by objdump; elf_validate_full_c retained for rollback + differential). The segment copy + mmap + PT_DYNAMIC relocation engine STAY in C (paging-entangled). Verified throwaway VM2808 (destroyed): desktop "v1.95.0 (build 808)" 2 differing screendumps (pixel-diff after input injection) + idle RIP=0x58516a CPL=0 HLT=1 (no busy-spin) + [RUST-DIFF] elf 256 0 mism PASS + [RUST-SEC] elf 100/100 x3 + [RUST-PERF] elf C=35/RS=154 ratio=4.40. STRONG LIVE PROOF: a full desktop = the compositor + every desktop app ELF was validated through elf_validate_full_rs on the way up (serial shows many live `[ELF] Validation passed: executable, 1 program headers, 1 PT_LOAD`). Golden + VMs 2200/2251/106/2160 untouched. **CORRECTION 2026-07-16 (3-way drift audit):** this seam is a **MERGED seam that CHANGED A PUBLIC API'S CONTRACT**, which the "drop-in with identical behavior" framing does not cover and this row did not state. The extraction moved the PT_LOAD file-bounds check and the "no PT_LOAD" check OUT of `calculate_load_bounds()` and INTO `elf_validate()`. For the 3 loaders that is invisible (0 mismatches / 438,747 vectors, since they always ran both), but **`elf_validate` is public and 8 OTHER callers never call `calculate_load_bounds`** (proc/cron.c:419, proc/services.c:235, proc/syscall.c:1435, proc/process.c:1682, desktop.c:96, gui/desktop.c:121, net/remote_ctrl.c:1048/1070/1244, proc/selfupdate.c:173) and inherit a STRICTLY STRONGER check: **48,130/438,747 vectors change verdict** (16,896 -> NO_PHDR, 31,234 -> SEGMENT_OVERFLOW), **0 in the accept direction** = fail-closed, so no binary that used to load is now rejected. Only proc/selfupdate.c:173 is genuinely observable (validate-only shape gate on a new kernel image; now also requires >=1 in-bounds PT_LOAD; a real kernel.elf passes and the path is SHA256+RSA gated). Contract change now DOCUMENTED at elf_validate()'s declaration in exec/elf.h (where callers see it) and at the dispatcher in exec/elf.c. **The -DRUST_ELF flag is NOT a behavior rollback**: both flag states carry the merged contract; the flag only chooses which implementation enforces it. **#489 C-FALLBACK HARDENING LANDED (2026-07-16):** `check_overflow_add(a,b,max)` was `a > max-b`, which UNDERFLOWS when b>max and reported "no overflow", so dropping -DRUST_ELF reintroduced advisory 0003's OOB read+WRITE; now `(b > max) || (a > max-b)`. ZERO REGRESSION by construction: for b<=max the expression is IDENTICAL to the original, so no loadable image can change verdict; it differs only where b>max, i.e. already-malformed images, and fail-closed. `elf_validate_full_c` deliberately keeps the ORIGINAL underflowing arithmetic via a new `check_overflow_add_verbatim()` so it stays a faithful reference of the pre-port verdicts while the LOADER path (calculate_load_bounds/elf_vaddr_to_fileptr/elf_parse_dynamic) is sound independent of the flag. NOT BUILT/BOOTED (isolated parallel-dev). |
| exec/pe.c pe_validate (PE32 pre-map validation: DOS MZ + PE signature + COFF Machine + OptionalHeader Magic/exec/section-count + section-table raw/virtual-bounds walk) | b809 | ~55 (pe_validate header checks + pe_load per-section raw/virtual bounds refactored verbatim into pe_validate_full_c, incl. the absent section-table bounds check + the uint32 overflow arithmetic) | ~210 (pe_validate_full_rs + PeInfo #[repr(C)] sizeof asserted ==32 + reused bounds-checked LE readers; 4 _Static_assert lock the DOS/FILE/NT/section header sizes so the Rust field offsets + PE_NT_HEADERS32_SIZE=248 can never drift) | 256 boot (well-formed synthetic PE32 + shared DOS/PE/COFF/OptionalHeader/section reject mutations: bad MZ, e_lfanew past EOF, bad PE sig, wrong Machine, PE32+ magic, not-executable, 0 sections, truncated, section raw past EOF; rs==c) 0 mism; +2,000,000 offline pre-flight (well-formed + all reject classes + random-header fuzz, 0 agreement mism; 429,242 accepts) + C reference ASan+UBSan-CLEAN over the whole 2M AND ASan-PROVEN heap-buffer-overflow READ in pe_validate_full_c (section table extended past an exact-size heap buffer) + modeled pe_load memcpy OOB-READ SEGV on the uint32-raw-overflow section | C=29 -> RS=124 cyc/op, ratio=4.27 (RDTSC; HONEST: RS ~4.3x SLOWER - same profile as elf b808: the C is a trivial inlined field-read walk that -O2 folds to ~29 cyc, the Rust crosses FFI + bounds-checks every read + walks the section table; PE load is a COLD once-per-launch path, so ~95 extra cycles is negligible; the value is memory-safety-by-construction, not speed) | **SEVENTH Tier-2 untrusted-input parser. HONEST: LATENT not reachable-from-boot (unlike elf/dhcp/ext2) because pe_load has NO live caller today - the Win32 subsystem (#288) is unimplemented - so these are a PRE-EMPTIVE confinement for when #288 wires PE loading, provable offline under ASan but not currently attacker-reachable in the running system.** Removes THREE OOB classes by construction (slice of exactly `size` bytes, a section-table-fits bound, widened u64 per-section arithmetic): (1) NO SECTION-TABLE BOUNDS CHECK - pe_get_sections locates the table at &OptionalHeader+SizeOfOptionalHeader and pe_load reads sections[i] blind, so a bogus SizeOfOptionalHeader / oversized NumberOfSections OOB-READS the section table (ASan-proven heap-buffer-overflow READ of 4 bytes at the section-loop line on an exact-size buffer; Rust bounds num_sections*40 <= size-sect_base first); (2) uint32 PointerToRawData+SizeOfRawData > size - an overflowing sum wraps below `size` and PASSES, then pe_load's memcpy(dest, pe_data+PointerToRawData, copy) OOB-READS the file buffer (modeled memcpy SEGV offline; Rust uses widened u64); (3) uint32 VirtualAddress+SizeOfRawData > SizeOfImage - overflowing sum passes, then pe_load's memcpy OOB-WRITES past the kmalloc'd image (the ELF-p_filesz write class; Rust widened). Note the seam is STRICTER than the live loader on purpose: pe_load merely SKIPS a bad section, the seam REJECTS it. [RUST-SEC] pe (live boot): 100/100 section-table-overrun + 100/100 raw-uint32-overflow + 100/100 vaddr-uint32-overflow, C-accepts/over-reads + Rust-confined; offline 143,400 raw + 143,099 vaddr EXPECTED divergences (C accepts, RS rejects) over 2M. Candidate plain-C fix for #288 (add the table-fits bound + widen the two per-section adds); flagged in blame.md. | LIVE (pe_validate -> 0x53cfca `call 5e9210 <pe_validate_full_rs>` proven by objdump; pe_validate_full_c retained for rollback + differential). The section copy + import resolution + relocation STAY in C (pe_load/pe_execute). Verified throwaway VM2809 (destroyed): healthy desktop b809 (auto-login + compositor + heartbeat top=idle:99 + advancing uptime 74s->195s/ctxsw/flips) + 2 DIFFERING screendumps (f6bc7e.. vs 1b4c2f.. clock-tick redraw) + idle RIP=0x585b8a CPL=0 HLT=1 (no busy-spin) + [RUST-DIFF] pe 256 0 mism PASS + [RUST-SEC] pe 100/100 x3 + [RUST-PERF] pe C=29/RS=124 ratio=4.27; [RUST-DIFF] elf still PASS (ELF path unaffected = no regression). LIVE PE-LOAD NOT EXERCISABLE (HONEST): pe_load has no caller on the golden (Win32 #288 unimplemented) - no PE app launches - so pe_validate_full_rs is proven by the differential + 2M offline + ASan, NOT a live PE launch; a clean desktop boot confirms zero regression regardless (ELF/boot path untouched). Golden + VMs 2200/2251/106/2160 untouched. |
| fs/fat.c fat_dir_step (VFAT directory per-entry decode + long-file-name (LFN) reassembly: 0x00 end / 0xE5 deleted / e[11]==0x0F LFN-fragment accumulate with the (seq-1)*13<255 bound + lfn_extract UCS-2->ASCII / short-entry 8.3-or-long name select) | b810 | ~22 (the decode body lifted VERBATIM out of the fat_readdir_inner loop into fat_dir_step_c; the b<0/b==0x00 disk-return gating stays in readdir) | ~120 (fat_dir_step_rs + FatLfnState/FatParsedEntry #[repr(C)] sizeof asserted ==264/==292 + lfn_extract_rs + fat_name_to_str_rs) | boot [RUST-DIFF] fat 747 files 0 mism (random well-formed dirs 1..3 files, name len 1..250, LFN sets + 8.3 + 0xE5-deleted + orphan-LFN + bad-seq 0/63 mutations; rs==c on names+count) + 3,000,000 OFFLINE pre-flight: 2,000,000 well-formed dirs (4,002,068 files) 0 mism byte-identical names + 1,000,000 malformed dirs 0 structural mism (0xE5 / orphan / bad-seq / truncated-set / over-long 256..295) + C reference ASan-PROVEN heap-buffer-overflow (WRITE size 260 into a 256-byte buf via the downstream strcpy) vs Rust confined clean | C=543 -> RS=480 cyc/walk, ratio=0.88 (RDTSC over a fixed 2-file dir; RS ~12% FASTER - the len-bounded slice copies fold better than the C's per-entry strcpy under -fno-builtin; FAT readdir is a warm boot + file-manager path) | **EIGHTH Tier-2 untrusted-input parser and a genuinely REACHABLE overflow on the LIVE FAT-root boot path (same value class as ext2 #476; stronger than arp/dns/url/pe latent cases).** The C's INTERNAL longname[260] reassembly is itself bounded (the (seq-1)*13<255 guard caps the last write index at 259, so no intra-buffer overflow) - so the reassembly port is defense-in-depth. The REACHABLE bug is DOWNSTREAM: fat_dir_step_c can EMIT a name of up to 259 chars, and fat_readdir_inner then `strcpy(name_out, name)` into caller buffers that are only 256 (SYS_READDIR name_buf[256] - USERLAND-reachable), 64 (fat_delete's dir-empty scan - reachable via rmdir), or 16 (dos/dosexec find) bytes. A crafted FAT directory (attacker-supplyable: the golden is FAT-root, so a malicious USB stick / disk feeds these bytes) with a 256..259-char VFAT long name overflows name_buf[256] by up to 4 bytes; the 64/16 callers overflow on any normal long name. ASan PROVES the heap-buffer-overflow (WRITE size 260 into 256). [RUST-SEC] fat (live boot): C emits >255-char name 200/200, Rust confined 0/200 (the Rust caps the decoded name at FAT_NAME_MAX_RS=255 so the downstream copy can never exceed the 256-byte contract). PLAIN-C FIX applied alongside: fat_delete's char name[64] -> [256] (flagged in blame.md; dos/dosexec char namebuf[16] flagged, left to the DOS owner). On real entries (names <256) rs==c byte-identical. | LIVE (fat_readdir_inner -> 0x4c4de9 `call 5e8430 <fat_dir_step_rs>` proven by objdump; fat_dir_step_c retained for rollback + differential). The cluster-chain walk + sector read (dir_read_entry) + the write/create/rename path + fat_lookup (app-launch path-resolve; its internal buffer is equally bounded - noted, not yet ported) STAY in C. Verified throwaway VM2810 (destroyed): healthy desktop b810 (auto-login root + compositor DESKTOP_READY + heartbeat top=idle:99 + advancing uptime 76s->214s / ctxsw / flips 525->4341) + 2 DIFFERING screendumps (a3907f.. vs d0f65c.. clock-tick redraw) + idle RIP=0x58667a in idle_process/sched_schedule CPL=0 (HLT idle, no busy-spin) + [RUST-DIFF] fat 747 0 mism PASS + [RUST-SEC] fat 200/200 confined + [RUST-PERF] fat C=543/RS=480 ratio=0.88. STRONG LIVE PROOF: a full FAT-root desktop = the kernel loaded every real FAT directory (ttf_init /FONTS scan at boot + SYS_READDIR + file-manager enumeration) through fat_dir_step_rs; a healthy desktop means the Rust parser decoded all the real directories byte-correctly. Golden + VMs 2200/2251/106/2160 untouched. |
| fs/exfat.c exfat_dir_step (exFAT directory ENTRY-SET decode: File 0x85 + Stream 0xC0 + Name 0xC1 -> reassembled UTF-16 name -> UTF-8) | b811 | ~65 (parse_entry_set body lifted VERBATIM into exfat_dir_step_c; the `fs` param was unused, so genuinely pure - no disk I/O, no FS mutation) | ~120 (exfat_dir_step_rs + ExfatDirInfo #[repr(C)] sizeof asserted ==296; reuses elf_rd_u16/u32/u64) | boot [RUST-DIFF] exfat 366 files 0 mism + 3,000,366 OFFLINE (866,937 well-formed byte-identical + 1,383,513 malformed, 0 structural mism) + C reference ASan/UBSan-clean over the 3M | C=434 -> RS=540 cyc/walk, ratio=1.24 (RDTSC; HONEST: RS ~24% SLOWER; cold/latent dir path) | **NINTH Tier-2 untrusted-input parser. HONEST: the C does NOT overflow (unlike fat b810) - it BOUNDS SecondaryCount (offset+bytes_used>buf_size reject) and CAPS the name reassembly (name_offset+j<255 into name_buf[256]).** The only residual defect is the UNINITIALIZED name_buf -> a crafted NameLength>present-entries reads STALE STACK into the filename (info-leak, NON-OOB), which the Rust confines by zero-init + bounds-checked slice reads. [RUST-SEC] C stale-stack leak 7/200 (dirty-stack witness, non-deterministic), Rust confined 0/200; oversized-SecondaryCount both reject 200/200; ALSO an ASan-witnessed LATENT sub-32 header-read OOB (not reachable via the real 32-aligned caller) that the Rust confines. REACHABILITY: exfat_mount is reachable via USB hot-plug but exfat_readdir has NO live caller on the FAT32-root golden - REACHABLE-but-LATENT (exec/pe.c class). NO plain-C overflow fix (no overflow); the stale-stack read flagged to blame.md as low-sev latent hardening. | LIVE routing (exfat_readdir -> exfat_dir_step -> exfat_dir_step_rs, objdump-proven); exfat_dir_step_c kept for rollback + differential. Cluster-chain walk + cluster read + write/open path stay C. Verified throwaway VM2477 (destroyed): healthy desktop "v1.95.0 (build 811)" + 2 differing screendumps (desktop vs animating idle screensaver) + idle RIP=0x58736a CPL=0 HLT=1; exFAT-readdir NOT exercisable (golden mounts no exFAT vol) so proven by differential+offline+ASan not a live listing; FAT/boot path unaffected = no regression. Golden + VMs 2200/2251/106/2160 untouched. Backup the kernel build container:/root/rust-port-backups/kernel-pre-exfat-b810-20260715_222055.tar.gz. kernel.elf md5 12b86f06aca18f921ec1890846bee7b6. |
| gui/image.c bmp_decode (BMP image decode: BITMAPFILEHEADER magic/bfOffBits + BITMAPINFOHEADER width/height/bpp/compression parse + 24/32bpp uncompressed bottom-up/top-down BGR(A)->0x00RRGGBB pixel decode) | b812 | ~90 (the header-parse+validate+pixel-decode lifted VERBATIM out of image_load_bmp into the pure seam bmp_decode_c, decoding into a caller-bounded out_px/out_cap_px buffer instead of a self-alloc; out_px==NULL = validate+dims-only) | ~130 (bmp_decode_rs + reused elf_rd_u16/u32 bounds-checked LE readers; all scalars+out-pointers cross FFI so NO #[repr(C)] struct to size-lock; signature asserted-by-use in gui/image.c) | boot [RUST-DIFF] bmp 256 vectors 0 mism PASS + OFFLINE: the REAL golden boot.bmp (1080x720 24bpp) PIXEL-IDENTICAL 777,600 px 0 diff (CRITICAL) + 40,000 synthetic well-formed 24/32bpp bottom-up+top-down 0 mism + 3,000,000 malformed (width*height overflow / huge dims / bfOffBits past EOF / row-stride past EOF / truncated pixel data / bad bpp / RLE compression / bad signature / header_size<40 / 65536x65536 uint32-wrap / random-header fuzz) 0 divergences + C reference ASan-CLEAN over the malformed corpus (0 heap errors = NO reachable OOB) + UBSan witness of the C's INT32_MIN `-height` signed-overflow UB (benign) | C=3038-3241 -> RS=5170-5351 cyc/op, ratio=1.65-1.70 (RDTSC over a 32x32 24bpp decode; HONEST: RS ~65-70% SLOWER - the per-pixel bounds-checked decode loop vs the C's raw pointer walk under -fno-builtin/soft-float/-mno-sse; BMP decode is a COLD wallpaper/boot/preview path, so the delta is negligible; value = memory-safety-by-construction) | **TENTH Tier-2 untrusted-input parser and the FIRST IMAGE DECODER (the densest OOB surface - untrusted pixel bytes from wallpapers/downloads/previews). HONEST: LATENT/defense-in-depth (arp/dns/url tier), NOT a reachable OOB - one of the SAFER C references.** The classic BMP undersized-alloc integer-overflow OOB WRITE is NOT reachable: image_load_bmp sizes the output as a 64-bit (size_t)width*height*4 (no overflow for int32 dims: max (2^31-1)^2*4 < 2^64) that kmalloc rejects if it exceeds the 256MB heap, so a huge image fails alloc BEFORE any decode and the write is always bounded by an accurate allocation; and the input reads are bounded by the accurate pixel-data gate for sane dims. Reaching the input over-read needs ~10^9 output pixels (to wrap the uint32 row_size/pixel_data_size arithmetic past 2^32), which the 64-bit alloc/capacity gate rejects first - ASan over 3M malformed = 0 heap errors confirms no reachable OOB. The Rust removes the integer-overflow CLASS BY CONSTRUCTION (u64 checked row_size/pixel_data_size + capacity math, immutable slice of exactly len bytes, every pixel-source read + output write bounds-checked, defined wrapping_neg for INT32_MIN height): it REJECTS the impossible/lying header at the PARSE seam where the C's uint32 wrap ACCEPTS it and defers to a doomed kmalloc. [RUST-SEC] bmp (live boot): C accepts impossible header (uint32 size wrap) 250/300, Rust confines 300/300; offline 3M malformed 0 divergences. NO plain-C overflow fix warranted (no reachable overflow); the INT32_MIN `-height` UB flagged to blame.md as low-sev C hardening. | LIVE (image_load_bmp inlines a direct `call 5e8d80 <bmp_decode_rs>`; the dispatcher bmp_decode @0x4f66e0 tail-`jmp 5e8d80 <bmp_decode_rs>`, both objdump-proven; bmp_decode_c @0x4f64e0 retained for rollback + differential). The framebuffer blit/scale + image_load dispatch (PNG/JPEG) STAY in C. Throwaway VM2812 (fresh 1G LV copy of golden, b812 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64): booted to a healthy desktop v1.95.0 (build 812) with THE REAL SWIRL WALLPAPER RENDERED (BACK.BMP 1280x720 blue/purple/teal swirl + logo + starfield, decoded pixel-perfect by bmp_decode_rs - serial `[Desktop] Background image loaded: 1280x720`, NOT the gradient fallback) + icons + taskbar + CPU/RAM/DSK/NET + HA Sun widget; 2 DIFFERING screendumps (animated idle screensaver = live) + a woken-desktop wallpaper frame; idle RIP=0x587a9a CPL=0 HLT=1 (no busy-spin); all boot self-tests PASS incl. the 3 bmp lines; the 20 prior [RUST-DIFF] still PASS (no regression). STRONGEST LIVE PROOF OF THE SERIES: BOTH the 3MB BOOT.BMP boot splash AND the 2.7MB BACK.BMP wallpaper decoded through bmp_decode_rs on the way up - a pixel-perfect swirl on the running desktop = the Rust decoded the real image byte-correctly. (First boot hit the pre-existing #264 RIP=0x45 spawn race AFTER all bmp selftests passed + BOOT.BMP decoded; a retry booted clean.) Golden + VMs 2200/2251/106/2160 untouched; VM2812 + LVs purged. Backup the kernel build container:/root/rust-port-backups/kernel-pre-bmp-b811-20260715_225656.tar.gz. kernel.elf md5 fd0c0c52aac182574603ef4a4b10063a. |
| gui/png.c png_parse_ihdr + png_defilter (PNG IHDR validate + None/Sub/Up/Average/Paeth per-scanline defilter reconstruction; zlib/inflate stays C) | b813 | ~40 (IHDR field-validate + uint32 scanline_len/raw_size math lifted verbatim into png_parse_ihdr_c; the per-scanline defilter loop into png_defilter_c over a caller-BOUNDED out) | ~150 (png_parse_ihdr_rs + png_defilter_rs + png_unfilter_row_rs + png_paeth_rs + PngInfo #[repr(C)] sizeof asserted ==32) | boot [RUST-DIFF] png 512 (256 IHDR-parse rs==c on accept+all-8-fields + 256 defilter rs==c on output bytes, all 4 color types x 5 filters) 0 mism + OFFLINE: 224 REAL PNGs (all 4 color types x {mix,rand,0,1,2,3,4} filters) PIXEL-IDENTICAL 398,300 px, rs==c AND ==python-reference-expected (CRITICAL) + 3,000,000 parse + 3,000,000 defilter differential 0 mism + C reference ASan-PROVEN heap-buffer-overflow WRITE (size 16, png_defilter_c) + READ (size 16) + ASan-PROVEN downstream convert-loop OOB from png_parse_ihdr_c's uint32 wrap | C=2145 -> RS=2217 cyc/op, ratio=1.03 (RDTSC 32x32 RGBA defilter; PARITY, RS ~3% slower; PNG decode is a cold icon/preview/download path) | **ELEVENTH Tier-2 and the SECOND IMAGE DECODER - the marquee OOB surface (libpng CVE class), with a genuinely REACHABLE integer-overflow OOB (read + WRITE) in the C reference (ext2/fat class; STRONGER than bmp's latent case).** The C's UNVALIDATED IHDR width/height feed UINT32-WRAPPING size math (png_parse_ihdr_c: scanline_len=width*bpp, raw_size=(scanline_len+1)*height); a crafted IHDR (width=0x40000000 RGBA) wraps scanline_len to 0 and raw_size/pixel-alloc to tiny, and the downstream height-row/width-column decode loops then walk far past the wrapped-small heap allocations => heap OOB READ (inflated/scanline) + OOB WRITE (pixels). ASan-PROVEN: png_defilter_c heap-buffer-overflow WRITE (size 16) + READ (size 16) on hostile/truncated inputs, and the modeled image_load_png downstream convert-loop OOB from the png_parse_ihdr_c-accepted wrap (heap-buffer-overflow READ, first iteration). Rust removes the class BY CONSTRUCTION: png_parse_ihdr_rs does CHECKED u64 size math and REJECTS the impossible header AT PARSE (before any alloc/loop, confining it in the live -DRUST_PNG path); png_defilter_rs bounds every inflated read against inflated_len and every output write against out_cap (slices of exactly the given lengths). [RUST-SEC] png (live boot): C accepts impossible IHDR (uint32 wrap) 300/300, Rust confines 300/300 at parse; defilter Rust confines 200/200 hostile. REACHABLE: PNG is genuinely reachable untrusted input (browser <img>, Files image previews, downloaded/wallpaper images all feed image_load_png). **#500 DONE 2026-07-16 (plain C) - FIX B (the STRONG fix), NOT FIX A.** Two candidate fixes existed and they are NOT equivalent. **FIX A** = cap the caller-side product at gui/png.c:659-660 (`defilt_cap = scanline_len * height` + `kmalloc(defilt_cap ? defilt_cap : 1)`). Measured by the dedicated PNG auditor: FIX A drives extraction drift to 0, **but leaves the C fail-closed-but-STRICTER than the original in 24 wrap cases and leaves 48 wrap cases STILL UNSAFE**, so advisory 0005's underlying class would stay LIVE in plain C on any -DRUST_PNG-off rollback. That is the exact "rollback restores the flag, not the behavior" defect already found in three other seams, so FIX A was REJECTED: closing the drift metric while leaving the class live is the wrong trade. **FIX B (LANDED)** = checked u64 math in png_parse_ihdr_c, identical to png_parse_ihdr_rs, which makes the C **sound independently of -DRUST_PNG** and collapses the twin onto the Rust. The caller-side cap at :718-719 is deliberately left as plain uint32 because FIX B makes it EXACT by construction (checked (scanline_len+1)*height fits u32 implies scanline_len*height does), so capping it too would be dead code. Landed fix verified semantically identical to the auditor's independently-implemented png_fixB.c variant. This was NOT cosmetic: the b813 extraction re-represented the defilter output from the original's TWO SWAPPING SCRATCH SCANLINES (each exactly scanline_len) into ONE PACKED scanline_len*height buffer - a uint32 product that exists NOWHERE in the pre-extraction original - and `kmalloc(defilt_cap ? defilt_cap : 1)` turned a wrap-to-zero (which heap.c's own size==0 -> NULL gate would have caught) into a valid 1-BYTE buffer that the defilter then wrote 32 KB into. **The ORIGINAL's defilter could not OOB-WRITE at all** (its memcpy destination was always exactly scanline_len), so this row's ASan-proven "WRITE (size 16, png_defilter_c)" was a property the EXTRACTION INTRODUCED, not one it inherited - and the twin was strictly MORE DANGEROUS than the pre-port C on a flag-off build. Measured after the fix: DRIFT (fixed C vs Rust) 0, BOTH-UNSAFE 0, valid-domain regression 0 over 300,000 headers, and defilt_cap wraps 183,321 -> 0. Since checked (scanline_len+1)*height fits u32 implies scanline_len*height does, defilt_cap is now exact BY CONSTRUCTION and rollback is faithful. | LIVE (image_load_png inlines `call 5ecd40 <png_parse_ihdr_rs>` + `call 5ec8d0 <png_defilter_rs>`; dispatchers png_parse_ihdr/png_defilter tail-jmp them, all objdump-proven; png_parse_ihdr_c/png_defilter_c retained for rollback + differential). The zlib/inflate core + chunk-CRC walk + IDAT accumulation + BGRA convert STAY C. Throwaway VM2813 (fresh 1.2G LV copy of golden, b813 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64): healthy desktop v1.95.0 (build 813) (swirl wallpaper + icons + taskbar + HA Sun widget populated = live net) + liveness triad (idle RIP=0x58854a CPL=0 HLT=1, heartbeat 30419->30923, RAX churn 0x75a0->0x784c) + animated screensaver differing-md5 pair; [RUST-DIFF] png 512 0 mism PASS + [RUST-SEC] png 300/300+200/200 + [RUST-PERF] png C=2145/RS=2217 ratio=1.03; all 21 prior [RUST-DIFF] still PASS (22 total, 0 FAIL = no regression). HONEST: live ON-SCREEN PNG render NOT triggered - the golden ships ZERO PNG assets (all wallpapers/icons are BMP; the wallpaper loader is BMP-only) and this iMac-USB-HID kernel does not accept headless input, so the live Rust-PNG proof is the boot self-test running the REAL compiled seams (512 vectors, real 5-filter reconstruction) + routing + the 224 real-PNG offline pixel-identical, NOT an on-desktop PNG render. Golden + VMs 2200/2251/106/2160 untouched; VM2813 + LVs purged. Backup the kernel build container:/root/rust-port-backups/kernel-pre-png-b812-20260715_235301.tar.gz. kernel.elf md5 4efec848159316077164b1563800f119. |
| gui/jpeg.c jpeg_parse_headers (baseline-JPEG header/segment parse seam: SOI/APPn/COM/DQT/DHT/SOF0/SOS marker+segment-length walk -> jpeg_hdr_t; entropy/Huffman/IDCT/MCU-upsample/color-convert decode stays C, gated behind it) | b814 | ~120 (the in-line marker loop + parse_dqt/parse_dht/parse_sof0 + the SOS scan-header read lifted VERBATIM into jpeg_parse_headers_c over a raw (data,len,pos) cursor; read_byte/read_u16 + parse_sos-header removed) | ~330 (jpeg_parse_headers_rs + JCur bounds-checked cursor + jpeg_dqt/dht/sof0/sos_rs + JpegHdr #[repr(C)] sizeof asserted ==1512) | boot [RUST-DIFF] jpeg 63 vectors 0 mism (real 435-byte 4:2:0 JPEG + ~64 truncations, byte-identical jpeg_hdr_t) + OFFLINE: 280 REAL baseline JPEGs (4:4:4/4:2:2/4:2:0/grayscale, dims 1x1..120x80, q5..q95) jpeg_hdr_t BYTE-IDENTICAL rs==c + ~3,000,000 differential (real+truncations+mutation-fuzz): WELL-FORMED-IDENT 280 + GENERIC-AGREE 2,913,326 + CONFINE 111,195, 0 hard mismatches (0 rs-permissive, 0 both-accept-field-diff, 0 non-confinement code-diff) + C reference ASan-CLEAN on all safe vectors AND ASan-PROVEN on all 3 crafted OOBs | C=580 -> RS=607 cyc/op, ratio=1.04 (RDTSC over the real 435-byte header parse; PARITY, RS ~4% slower; JPEG decode is a cold album-art/preview/img path) | **TWELFTH Tier-2 and the THIRD IMAGE DECODER. HONEST: REACHABLE (ext2/fat/png class, NOT latent) - THREE ASan-PROVEN reachable OOBs in the C reference, one an OOB WRITE.** (A) SOF0 comp_qt UNVALIDATED -> decode_block indexes quant[comp_qt][k] (quant is [4][64]); comp_qt is a full byte (0..255) -> past-allocation heap OOB READ (ASan-proven). (B) SOS comp_dc/comp_ac UNVALIDATED (0..15) -> decode_huff indexes huff_fast[dc][comp_dc] ([2] deep) -> past-allocation heap OOB READ (ASan-proven). (C) DHT sum-of-16-count-bytes (up to 4080) written into huff_vals[.][.][256] with NO clamp -> heap OOB WRITE (ASan-proven: WRITE size 1 at the 1512-byte struct boundary, in hdr_parse_dht - the severe one). JPEG is genuinely reachable untrusted input (album art in the music player, Image Viewer, browser <img>, Files previews all feed image_load_jpeg). The Rust rejects comp_qt>=4, comp_dc/ac>=2, and DHT total>256 at the parse seam BEFORE any decode/alloc; under -DRUST_JPEG the live image_load_jpeg gates decode on jpeg_parse_headers_rs, so all three are CONFINED live = a live security improvement. comp_h/comp_v left unvalidated to match C exactly (garbage sampling is NOT an OOB in the C decoder - block loops are bx<h/by<v gated, target/pixel writes bounds-checked). [RUST-SEC] jpeg (live boot): (A) C accepts 100/100 Rust confines 100/100; (B) C accepts 100/100 Rust confines 100/100; (C) Rust confines 100/100 (C OOB WRITE ASan-proven OFFLINE, never run in-kernel). Candidate plain-C hardening: #501 (clamp DHT total<=256 + validate comp_qt<4 + comp_dc/ac<2). **ADDED 2026-07-16 (#404 drift audit 2 + live-fix): this row MISSED A LIVE OOB WRITE that the Rust seam did NOT confine.** The b814 extraction moved `build_huffman` OUT of parse_dht and INTO image_load_jpeg, i.e. JUST OUTSIDE this seam's boundary, so the actual write site was never ported. Its `code` is unbounded for a NON-CANONICAL bits[]: `bits[0]=255` (255 one-bit codes; canonical max is 2) writes huff_fast[dc][idx][0..130559] into a [1024] array = **259,072 bytes past the array, 252,736 past the 9760-byte jpeg_decoder_t** (measured on an exact-size alloc, not estimated). It was **LIVE under the shipped -DRUST_JPEG** because a count-sum of 255 is <= 256 and `jpeg_dht_rs`'s only gate was `total > 256`, so the seam ACCEPTED it and handed it straight to the write. Ring-3 reachable via sys_decode_image (browser `<img>`), Files previews, imageviewer, album art. PROVEN end-to-end through the real shipped seam under ASan (`jpeg_parse_headers_rs ret=0 ACCEPTED` -> `heap-buffer-overflow WRITE of size 2 ... 0 bytes to the right of 9760-byte region`). FIXED 2026-07-16 in plain C at the write site (canonicality bound + a `k >= 256` bound for vals[], flag-independent), and `jpeg_dht_rs` now rejects non-canonical tables too so both layers agree. Validated against an INDEPENDENT oracle (the Kraft inequality, which the incremental guard provably expands to): 300,000 vectors, C-vs-Kraft 0 mismatches, Rust-vs-Kraft 0, valid-domain regression 0, 4/4 negative controls fire. HONEST: build_huffman is BYTE-IDENTICAL between the pre-extraction original and the live tree (md5 903798ad...), so this is a PRE-EXISTING original bug, NOT extraction drift - the extraction only NARROWED its reach (the original fired it during parse_dht with no SOS needed; the live code needs a fully successful parse). Ticket #518 / MAYTERA-SEC-2026-0013. | LIVE (image_load_jpeg -> 0x5009ee `call 5ec790 <jpeg_parse_headers_rs>` proven by objdump; the dispatcher jpeg_parse_headers @0x500950 tail-jmps it; jpeg_parse_headers_c retained for rollback + differential). The entropy/Huffman-build/IDCT/MCU-upsample/color-convert decode (jpeg_decode_scan) STAYS C, a later seam. Throwaway VM2814 (fresh 1G LV copy of golden, b814 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64): healthy desktop v1.95.0 (build 814) (swirl BMP wallpaper + icons + taskbar + HA Sun widget "above_horizon" populated = live net) + 2 DIFFERING screendumps (live animation) + idle RIP=0x588cba CPL=0 HLT=1 (no busy-spin); [RUST-DIFF] jpeg 63 0 mism PASS + [RUST-SEC] jpeg 3-OOB confinement + [RUST-PERF] jpeg C=580/RS=607 ratio=1.04; all 22 prior [RUST-DIFF] still PASS (23 total, 0 FAIL = no regression). HONEST: live ON-SCREEN JPEG render NOT triggered headlessly - the golden's wallpaper/boot splash are BMP and this iMac-USB-HID kernel refuses headless input (album art / Image Viewer need a user-opened file), so the live Rust-JPEG proof is the boot self-test running the REAL compiled seam (63 vectors on the real JPEG + truncations) + routing + the 280 real-JPEG offline byte-identical, NOT an on-desktop JPEG render. Golden + VMs 2200/2251/106/2160 untouched; VM2814 + LVs purged. Backup the kernel build container:/root/rust-port-backups/kernel-pre-jpeg-b813-20260716_003317.tar.gz. kernel.elf md5 519d4c642afd2a854443c25ca6851e5b. |
| gui/png.c inflate (DEFLATE/INFLATE decompression core: bit-reader + fixed/dynamic/stored block dispatch + canonical Huffman symbol decode + code-length RLE table build + LZ77 back-reference copy = the whole inflate() the PNG IDAT path calls) | b815 | ~140 (the whole static inflate(), kept verbatim as inflate_c; getbit/getbits/build_huffman/decode_symbol/fixed-tables stay C, still used by inflate_c) | ~330 (inflate_rs + InfState bit-reader + InfHuffman build/decode + length/dist/code-order tables; every window read/output write/input bit-read a bounds-checked slice, checked pos/dist/len widths) | 351 boot (27 embedded RAW-DEFLATE vectors fixed/dynamic/stored + overlaps at exact cap == embedded original + ~324 in-kernel mutations bit-flip/truncate/random-cap + 4 crafted hostile) 0 mism; OFFLINE 393 well-formed streams incl 22 REAL PNG IDAT rs==c==zlib BYTE-IDENTICAL + 3,500,000 fuzz (2.0M diff + 1.5M ASan) 0 mism + inflate_c ASan(1.5M+1.5M)+UBSan CLEAN (0 heap errors, 0 UB) + 4 hand-crafted classic attacks (dist-before-window/length-past-cap/max-dist-24577/bad-block-type) BOTH reject ASan-clean + overlap dist=1 len=5 -> 'CCCCCC' byte-identical | C=3427 -> RS=3443 cyc/op, ratio=1.00 (RDTSC; PARITY) | **LATENT / DEFENSE-IN-DEPTH - NO reachable OOB (unlike ext2/fat/elf/jpeg and unlike png-IHDR V).** The C inflate_c is a puff-style decoder that ALREADY bounds EVERY reachable OOB surface: the LZ77 back-ref copy checks BOTH dist>out_pos (rejects an OOB read before window start) AND out_pos+len>dst_cap (rejects an OOB write past the buffer); the literal write is gated out_pos>=dst_cap; the stored block checks input+output bounds; the dynamic code-length RLE guards every repeat with n<hlit+hdist; the canonical Huffman decode index stays in [0,total) by construction (code>=first invariant); input exhaustion returns -1. ASan over 3,000,000+ hostile vectors + the 4 hand-crafted classic attacks = 0 heap errors -> NO fold warranted, NO plain-C hardening task. Rust removes the WHOLE class by construction (bounds-checked slices HALT loudly, never corrupt) + explicit widths so nothing wraps. | THIRTEENTH Tier-2; the canonical DECOMPRESSION OOB seam. inflate_rs LIVE on the PNG IDAT path (objdump: image_load_png @0x13c1 `call inflate_rs`, dispatcher inlined), inflate_c kept verbatim for rollback + differential; chunk-CRC walk + IDAT accumulation + BGRA convert + png_parse_ihdr/defilter (V) unchanged. The #321 archiver + gzip content-encoding are USERLAND, so this kernel port covers the PNG image path only. Verified throwaway VM2401 (destroyed): healthy desktop "V1.95.0 (build 815)" (swirl BMP wallpaper + HA Sun widget) + 2 differing screendumps (input-driven) + idle RIP=0x58944a HLT=1; all 23 prior [RUST-DIFF] still PASS (24 total, 0 FAIL = no regression). HONEST: on-screen PNG decode NOT triggered headlessly (golden ships BMP wallpaper/splash, no PNG asset decoded at boot), so live proof = the boot self-test running the REAL compiled inflate_rs over real DEFLATE + routing + 22 real-PNG-IDAT offline byte-identity. Golden + VMs 2200/2251/106/2160 untouched. Backup the kernel build container:/root/rust-port-backups/kernel-pre-inflate-b814-20260716_013000.tar.gz. kernel.elf md5 c8e619d6b81ff5ebaf1cbd4341930010.
| net/tls/{tls,cert_store}.c TLS length-parse framing (record header + handshake-message walk + TLS1.2 bare cert-list walk + TLS1.3 CertificateEntry walk) | b816 | ~90 (the inline 5-byte record-header decode+bound in tls_recv_record; the two handshake-message header-read loops; the bare cert loop in cert_parse_chain; the CertificateEntry loop in tls13_parse_certificate_msg - all lifted verbatim/hardened into tls_parse_record_header_c / tls_hs_next_c / tls_cert_next_c / tls13_cert_next_c) | ~215 (tls_parse_record_header_rs + tls_hs_next_rs + tls_cert_next_rs + tls13_cert_next_rs; TlsRecordHdr/TlsHsMsg/TlsCertEnt #[repr(C)] sizeof-locked ==6/12/8; every length add/compare checked_add, every offset a bounds-checked slice index) | 352 boot (record+hs+cert12+cert13, well-formed + malformed) 0 mism; OFFLINE 3,000,000-vector differential (record 751,702 / hs 748,417 / cert12 749,355 / cert13 750,526) 0 mism ASan+UBSan CLEAN + 2 ASan OOB demonstrations of the ORIGINAL loop (READ 0 bytes past 1-byte body; READ 4094 bytes past 5-byte body) both while the seam cleanly rejects | C=42 -> RS=59 cyc/walk, ratio=1.40 (RDTSC over a 3-message walk; HONEST: RS ~40% slower - FFI + slice construction + null-checks per step vs C inline 64-bit compares; COLD per-connection handshake path, negligible absolute cost) | **FOURTEENTH Tier-2 and the FIRST TIER-1 REMOTE-reachable target. REACHABLE + REMOTE (the strongest of the series): a REMOTE/MITM-server pre-auth heap over-read in the ORIGINAL inline TLS 1.2 plaintext handshake loop.** That loop (run for EVERY handshake, incl TLS 1.3 up to ServerHello) guarded `pos<length` but read `data[pos+1..pos+3]` (up to 3 bytes past kmalloc(length)) AND passed an UNBOUNDED 3-byte hs_len into tls_process_server_hello() as its `length`, whose internal bounds then checked the ATTACKER-declared length not the real remaining bytes -> its 32-byte x25519 key_share memcpy + extension walk over-read far past the record allocation, on the server's FIRST flight before any cert/crypto check. Routing the loop through tls_hs_next() removes BOTH over-reads by construction (checked 4-byte header extent + checked body extent; hs_len is now guaranteed <= remaining record bytes so tls_process_server_hello can no longer read past the buffer either). ASan-PROVEN OFFLINE on the ORIGINAL loop (heap-buffer-overflow READ 0B past a 1-byte body + READ 4094B past a 5-byte body); the seam (rs AND c) returns 0/-1 clean on the same inputs. The record-header + TLS1.3-handshake-walk + TLS1.3-cert-walk were ALREADY bounded in C (defense-in-depth there); the reachable bug is specifically the TLS 1.2 plaintext hs loop. C-fallback hardening (so a -DRUST_TLS_PARSE ROLLBACK stays safe) = task #503. **A FOLD IS WARRANTED (remote-reachable pre-auth TLS over-read - the orchestrator should ship b816 to golden immediately).** | LIVE (dispatchers tls_parse_record_header/tls_hs_next/tls_cert_next/tls13_cert_next each tail-jmp their _rs [objdump R_X86_64_PLT32 tls_*_rs]; tls_recv_record calls tls_parse_record_header, both handshake loops call tls_hs_next, tls13_parse_certificate_msg calls tls13_cert_next, cert_store.c cert_parse_chain calls tls_cert_next; _c refs kept verbatim for rollback + differential; all crypto/AEAD/key-schedule/transcript-hash/X.509 DER parse STAY C). Verified throwaway VM2299 (destroyed, LVs purged): healthy desktop "v1.95.0 (build 816)" (swirl BMP wallpaper + HA Sun widget "above_horizon" populated = live net) + 2 differing screendumps (boot-splash -> desktop) + idle RIP=0x58a33a CPL=0 HLT=1 (twice stable = no busy-spin); [RUST-DIFF] tls_parse 352 0 mism PASS + [RUST-SEC] tls_parse (crafted-confinement OK, 512/512 malformed verdicts identical, 0 div, #503) + [RUST-PERF] tls_parse C=42/RS=59 ratio=1.40; all 24 prior [RUST-DIFF] still PASS (25 total, 0 FAIL = no regression). HONEST: a live end-to-end HTTPS handshake through the seam was NOT triggered headlessly (iMac-USB-HID kernel refuses headless input; no boot-time HTTPS fetch fired on serial this run), so live proof = the boot self-test running the REAL compiled seams over well-formed + malformed TLS bytes + objdump routing + 3M-vector offline real-framing identity + the 2 ASan OOB proofs; the seams ARE on the live handshake path for any HTTPS the desktop initiates. Golden + VMs 2200/2251/106/2160 untouched. Backup the kernel build container:/root/rust-port-backups/kernel-pre-tlsparse-b815-20260716_021749.tar.gz. kernel.elf md5 908011a1b27895a1c35bec59dbf521b1. |
| net/https.c + net/wget.c HTTP response length-parse (chunked chunk-size decode + framing-complete gate + header-block CRLFCRLF find + Content-Length digit parse) | b817 | ~110 (https_dechunk / https_chunked_is_complete / https_content_length / https_find_header_end lifted verbatim into *_c; wget find_header_end / parse_uint / http_decode_chunked into *_c) | ~300 (https_dechunk_rs + https_chunk_complete_rs + http_decode_chunked_rs + http_find_header_end_rs + https_content_length_rs + http_parse_uint_rs; all scalars+in/out pointers cross FFI, no #[repr(C)] struct; every size add checked_add, hex accumulation wrapping_* to match the C u32/size_t exactly, every copy a bounds-checked copy_within/slice) | boot [RUST-DIFF] http_parse 37 vectors (chunked bodies + incremental gate prefixes + header framing + Content-Length) 0 mism PASS + OFFLINE 5,270,310-vector differential (1.5M well-formed chunked decode both u32+size_t byte-identical + 1.27M incremental-gate prefixes + 1.5M malformed decode/gate + 500k header/CL + 500k parse_uint) 0 mismatches + C reference ASan-PROVEN heap-buffer-overflow in https_dechunk_c (memmove, __interceptor_memmove line, from a `kmalloc(body_len+1)` mirror) with the gate returning 1 (reachable), Rust confined (out=5<=len, gate rejects) | C=492 -> RS=540 cyc/decode, ratio=1.09 (RDTSC over a 255-byte single-chunk decode; HONEST: RS ~9% slower - byte-at-a-time bounds-checked loop vs the C raw pointer walk under -fno-builtin/soft-float; HTTP response parse is a per-fetch COLD path, negligible absolute cost) | **FIFTEENTH Tier-2 and the SECOND remote-reachable target (TLS class). REACHABLE + REMOTE: a u32 integer-overflow -> OOB read + WRITE in net/https.c https_dechunk (MAYTERA-SEC-2026-0008).** The chunk-size clamp `if (in + sz > len) sz = len - in;` is u32; a near-2^32 hex chunk-size makes `in + sz` WRAP below len so the clamp is skipped and the following `memmove(buf+out, buf+in, sz)` over-copies ~4 GiB out of the kmalloc'd HTTPS body (OOB read + WRITE). It is reachable behind https_chunked_is_complete (the crafted body "FFFFFFFE\r\n0\r\n\r\n" makes that gate's own u32 `p+sz` wrap back to a clean 0-chunk terminator, returning 1) so the decoder runs; a malicious/compromised HTTPS origin the browser visits or a compromised API/update endpoint sends it. ASan-PROVEN OFFLINE: https_dechunk_c heap-buffer-overflow (memmove) on the gate-passing evil vector; the Rust seam confines by construction (checked_add detects the wrap and takes the clamp, and the Rust gate rejects the frame as incomplete). The shared size_t http_decode_chunked (plain HTTP + https_post), find_header_end, and the Content-Length parse were ported alongside but the C is already bounded (size_t needs 2^64 to wrap and the `(end-read)<size` check rejects first) = LATENT/defense-in-depth. C-fallback hardening (so a -DRUST_HTTP_PARSE ROLLBACK stays safe) = task #504. **A FOLD IS WARRANTED (remote-reachable OOB WRITE on any chunked HTTPS response - ship b817 to golden).** | LIVE (https.c dispatchers https_dechunk/https_chunked_is_complete/https_find_header_end/https_content_length + wget.c find_header_end/parse_uint/http_decode_chunked each route their _rs under -DRUST_HTTP_PARSE; objdump R_X86_64_PLT32 to https_dechunk_rs/https_chunk_complete_rs/http_find_header_end_rs/https_content_length_rs in https.o and http_decode_chunked_rs/http_find_header_end_rs/http_parse_uint_rs in wget.o at the live https_get + wget_fetch + https_post call sites; _c refs kept verbatim for rollback + differential; socket recv + body buffering + redirect/SSRF gating STAY C). Verified throwaway VM2903 (destroyed, LV purged): healthy desktop DESKTOP_READY, idle:99 heartbeat progressing (uptime 113s), idle RIP=0x58ad9a CPL=0 HLT=1 (twice stable = no busy-spin), 2 differing screendumps (live). **STRONG LIVE PROOF: a real plain-HTTP fetch fired on the way up - `[HTTP] GET http://<HOME_ASSISTANT_HOST>:8123/api/states/sun.sun` -> `[HTTP] Status: 200 OK` + Content-Type/Content-Length/body parsed through the routed find_header_end_rs + parse_uint_rs path.** [RUST-DIFF] http_parse 37 0 mism PASS + [RUST-SEC] http_parse (C-gate=1 reaches vulnerable memmove; Rust gate=0 out=5 confined=OK; #504) + [RUST-PERF] http_parse C=492/RS=540 ratio=1.09; all 25 prior [RUST-DIFF] still PASS (26 total, 0 FAIL = no regression). Golden + VMs 2200/2251/106/2160 untouched. Backup the kernel build container:/root/rust-port-backups/kernel-pre-httpparse-b816-20260716_030001.tar.gz. kernel.elf md5 b449a6f4a2e00e5d19c493c812f74c18. |
| media/aac.c mp4_parse (AAC/M4A ISO-BMFF sample-table parse: descend moov/trak/mdia/minf/stbl -> stsd/mp4a/esds ASC extract + stsz/stco(co64)/stsc walk building the per-sample (offset,size) frame table; faad2 decode + ADTS + codec plumbing stay C) | b819 | ~105 (the whole mp4_parse + mp4_find + desc_len + mp4_extract_asc kept VERBATIM as mp4_parse_c for rollback + differential) | ~230 (mp4_parse_rs + mp4_find_rs/mp4_desc_len_rs/mp4_extract_asc_rs + mp4_rd_be32/be64 bounds-checked; Mp4Frame + Mp4ParseResult #[repr(C)], sizeof asserted ==8/==16 in aac.c; two-phase NULL-probe/fill to mirror the C kmalloc(nsamp)+fill) | boot [RUST-DIFF] mp4 256 vectors (well-formed .m4a atom trees: mixed stco/co64, uniform + per-sample stsz + reject cases) 0 mism PASS + OFFLINE 1,500,000-vector differential (random valid .m4a, frame table + ASC BYTE-IDENTICAL rs==c) 0 mism + C reference ASan-PROVEN heap-buffer-overflow READ (be32<-be64<-mp4_parse_c line 118, the stco/co64 chunk-offset walk; "0 bytes to the right of the 236-byte region") on a tight-malloc inflated-count .m4a, Rust confined the identical input (probe rr=1 nframes=6, no fault) | C=627 -> RS=714 cyc/op, ratio=1.13 (RDTSC over a fixed well-formed .m4a header parse; HONEST RS ~13% slower - the bounds-checked slice walk + two-phase probe overhead vs the C raw pointer reads under -fno-builtin/soft-float, AND the C reference here includes its kmalloc(nsamp)+free per op while the RS probe allocates nothing, so the real parse-core gap is larger; .m4a open is a COLD once-per-file path, negligible absolute cost) | **SIXTEENTH Tier-2 and the FIRST untrusted-MEDIA-FILE (non-image) parser. HONEST: REACHABLE (ext2/fat/elf class, local file) - MAYTERA-SEC-2026-0009 (CWE-125).** mp4_parse reads the sample-table COUNTS (nsamp from stsz, nchunks from stco/co64, nstsc from stsc) as trusted lengths and walks stsz_tab/stco_tab/stsc_tab by those counts with NO per-entry bound against the buffer: nstsc has NO clamp at all, nsamp/nchunks are clamped only to <= 10,000,000. A crafted .m4a whose declared counts far exceed the tables actually present drives be32(d + tab + i*stride) far past the kmalloc'd file buffer -> heap OOB READ; a runt stsz/stco/stsc atom at end-of-buffer also over-reads its own count field (be32(stsz+4/+8), be32(stco+4), be32(stsc+4)). REACHABLE from Ring-3: SYS_PLAY_WAV -> sys_play_wav -> audio_play_file_async -> audio_play_file (fat_read_file: attacker-controlled bytes off disk / a download) -> audio_decode_open -> aac_create -> mp4_parse, i.e. the music player opening any .m4a. The Rust slices exactly buf_size and every table/field read is a bounds-checked index, so an over-long count terminates the walk instead of reading past the buffer. [RUST-SEC] mp4 (live boot): verbatim C over-read past buf_size on 300/300 crafted inflated-count .m4a, Rust confined 300/300, 0 divergence-free. Candidate plain-C hardening (bound nsamp/nchunks/nstsc against the actual atom body + buf_size so a -DRUST_MP4 ROLLBACK stays safe) = task #505. **CORRECTED 2026-07-16 (#404 drift audit 2 + live-fix): mp4_parse_rs was NOT a pure confinement.** Its `.and_then(mp4a -> esds).or_else(esds in stsd)` chain fired the fallback in BOTH the "no mp4a" AND the "mp4a present but no esds inside" cases; the C (media/aac.c:196-213) is a strict if/else and only scans stsd when mp4a is ABSENT. That is an EXPANSION shipped under a confinement label: 22,571/600,000 (3.8%) of vectors, every one in the clean domain where the C provably reads nothing out of bounds, so not one could be excused as memory safety. It accepted files the C rejects and could select a different trak on multi-trak files. The self-test's builder could not express the state; it was found by READING the Rust. FIXED 2026-07-16: the chain is now a `match` mirroring the C's if/else exactly. **A FOLD IS WARRANTED (reachable local-file OOB READ on any played .m4a).** | LIVE (aac_create inlines the mp4_parse dispatcher -> objdump: two-phase `call 5f3c30 <mp4_parse_rs>` at 0x402d48 (probe) + 0x402e73 (fill) under -DRUST_MP4; mp4_parse_c.constprop.0 retained at 0x403170 for rollback + differential; faad2 decode + ADTS + codec plumbing stay C). Throwaway VM2930 (fresh 920M LV copy of golden b817, b819 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64, own efidisk): healthy desktop DESKTOP_READY + autologin OK + 2 DIFFERING screendumps (live animation, md5 6244764... vs 7eb16c9...) + idle RIP=0x58c2da CPL=0 HLT=1 (twice stable = no busy-spin) + heartbeat progressing idle:99 (uptime 111s); [RUST-DIFF] mp4 256 0 mism PASS + [RUST-SEC] mp4 300/300 confined + [RUST-PERF] mp4 C=627/RS=714 ratio=1.13; all 26 prior [RUST-DIFF] still PASS (27 total, 0 FAIL = no regression). HONEST: a live on-desktop .m4a PLAY through the Rust seam was NOT triggered headlessly (this iMac-USB-HID kernel refuses headless input and no boot-time .m4a decode fires), so the live Rust-mp4 proof is the boot self-test running the REAL compiled seam (256 well-formed + 300 crafted-OOB vectors) + objdump routing + the 1.5M offline byte-identical differential + the ASan-proven C OOB the Rust confines; the seam IS on the live play path for any .m4a the music player opens. Golden + VMs 2200/2251/106/2160 untouched; VM2930 + both LVs + temp image purged. Backup the kernel build container:/root/rust-port-backups/kernel-pre-mp4-b818-20260716_041641.tar.gz. kernel.elf md5 9ab82bb839cda9f23a10a88509bd31e1. |
| gui/jpeg.c jpeg_dequant_idct (baseline-JPEG entropy DECODE seam: the fixed 64-coefficient -> 64-sample transform at the decode_block tail = zigzag-scatter dequantize + integer inverse DCT (stb_image idct_block); the Huffman bit-reader / DC-AC coefficient decode / MCU-upsample / YCbCr->RGB write STAY C) | b822 | ~35 (idct_block + the inline dequant-scatter/DC-multiply of decode_block, lifted VERBATIM into jpeg_dequant_idct_c; decode_block now accumulates coeffs in zigzag order and calls the seam) | ~120 (jpeg_dequant_idct_rs + jpeg_idct_1d butterfly + jpeg_idct_block_rs + JPEG_ZIGZAG_RS; three raw pointers + fixed len 64, no #[repr(C)] struct crossed) | boot [RUST-DIFF] jpeg_idct 2000 realistic blocks (real luma quant table, DC+sparse AC) 0 mism PASS + [RUST-SEC] overflow-block c==rs IDENTICAL + OFFLINE 3,200,000 differential (1.5M real-quant realistic + 1.0M uniform + 0.7M extreme incl INT_MIN/MAX & quant 0/255) 0 mism (all 64 spatial samples byte-identical) + C reference ASan-CLEAN over all 3.2M (NO memory OOB) + UBSan-PROVEN reachable signed-integer-overflow in the C IDCT | boot [RUST-PERF] jpeg_idct C=589 -> RS=512 cyc/op ratio=0.86 (RDTSC; RS FASTER; JPEG decode is a cold album-art/preview/img path) | **NO reachable memory OOB (LATENT/DEFENSE-IN-DEPTH) + a REACHABLE CWE-190 signed-overflow UB the Rust well-defines.** Both C and Rust confine every access to the fixed 64-entry block (ASan-clean 3.2M). The genuine defect: the C IDCT does i32 signed-integer-overflow (UB, CWE-190) on large coefficient products; a crafted JPEG's entropy stream can carry ac_val up to +-32767 * quant up to 255, and the pass-2 accumulation overflows i32 even in the realistic band (UBSan-proven OFFLINE). On gcc -O2 x86-64 it wraps two's-complement (benign garbage pixels, 0 differential mismatch) but is UB the compiler may miscompile. Rust removes it BY CONSTRUCTION (bounds-checked slice indices) AND makes the arithmetic WELL-DEFINED via wrapping_* (byte-identical to the observed wrap). NOT an advisory (not memory corruption). Plain-C hardening (i64 IDCT intermediates) = task #507. | LIVE under -DRUST_JPEG_ENTROPY (image_load_jpeg inlines decode_block -> objdump: call <jpeg_dequant_idct_rs>; jpeg_dequant_idct_c retained for rollback + differential). Throwaway VM2931 (fresh 903M FAT copy of golden, b822 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64): healthy desktop DESKTOP_READY + autologin OK + 408520-unique-color render + 2 DIFFERING screendumps (654c819c vs 0d7fec75) + idle RIP=0x58d38a CPL=0 HLT=1 stable (no busy-spin); [RUST-DIFF] jpeg_idct 2000 0 mism PASS; all 30 [RUST-DIFF] PASS (0 FAIL = no regression). kernel.elf md5 46d985caaaea681473c5b5e0b0e9a896. |
| net/http2.c http2_get frame framing (HTTP/2 frame iterator: 9-byte header decode + 24-bit length-vs-buffer bound + per-type DATA/HEADERS padding strip + HEADERS PRIORITY strip, all bounds-checked; HPACK decode / flow-control / stream state STAY C) | b822 | ~45 (the inline per-frame framing + padding/priority strip of http2_get lines ~682-744 lifted VERBATIM into http2_frame_next_c (net/http2_frame_c.c); http2_get now assembles the 9+flen frame into one buffer and routes to the seam, using fr.data_off/fr.data_len) | ~130 (http2_frame_next_rs + H2Frame #[repr(C)] sizeof asserted ==28; checked_add on every derived length; slice of exactly len) | boot [RUST-DIFF] http2_frame well-formed frames 0 mism + evil zero-length PADDED: C-ref rc=1 (unchecked OOB read) Rust rr=-1 (CONFINED) PASS + LIVE=rust + OFFLINE 2.50M well-formed 0 mism + 2.0M structured-malicious + 3.0M random malformed (RS 0 ASan/UBSan traps) + C ASan heap-buffer-overflow (zero-len PADDED, h2frame_c.c) | boot [RUST-PERF] http2_frame C=65 -> RS=86 cyc/pass (RDTSC; RS ~1.3x, network path is IO-bound so negligible) | **REACHABLE REMOTE pre-auth OOB (CWE-476 rooted in CWE-125) = MAYTERA-SEC-2026-0010.** A malicious HTTPS site the browser visits negotiates ALPN h2 and sends one crafted zero-length PADDED DATA/HEADERS frame (flen==0, flag 0x08); the old inline code read payload[0]/pp[off] WITHOUT checking the frame carries a payload, and payload is only kmalloc'd when flen>0, so it is NULL -> Ring-0 NULL-deref -> kernel page fault -> whole-OS DoS. The seam REJECTS the frame (the http2_get refactor removes the inline read entirely). C-fallback hardening ticket #506. 3rd REMOTE advisory, first pre-auth Ring-0 NULL-deref DoS. **A FOLD IS WARRANTED (remote pre-auth DoS on any h2 site visited).** | LIVE under -DRUST_HTTP2_FRAME (http2_get -> objdump: call <http2_frame_next_rs>; http2_frame_next_c retained in net/http2_frame_c.c for rollback + differential). Throwaway VM2931 (same setup as jpeg row): [RUST-DIFF] http2_frame PASS + [RUST-SEC] MAYTERA-SEC-2026-0010 confined (evil C-ref rc=1 Rust rr=-1) + [RUST-PERF] C=65/RS=86; all 30 [RUST-DIFF] PASS (no regression). HONEST: a live remote h2 handshake through the seam was NOT triggered headlessly; the live-Rust proof is the boot self-test running the REAL compiled seam + objdump routing + the offline differential/ASan corpus; the seam IS on the live path for any h2 fetch. kernel.elf md5 46d985caaaea681473c5b5e0b0e9a896. **CORRECTION 2026-07-16 (3-way drift audit):** shipped (Rust) path independently RE-VERIFIED CLEAN (1.35M vectors, 0 unexplained divergence; the 4,272/600k ORIG-vs-RUST differences all classify to ONE intentional confinement, a length-0 PADDED frame on a non-1 stream, which RFC 7540 6.1 makes a PROTOCOL_ERROR anyway). **But the `_c` twin is NOT a faithful rollback target and this row implied it was.** The extraction RE-REPRESENTED the frame AT THE CALL SITE: header + a separately kmalloc'd payload (NULL when flen==0) became ONE contiguous `kmalloc(9+flen)`. That is a CALLER-side change both flag states inherit, so `http2_frame_next_c` CANNOT reproduce the original no matter how it is written. Concretely, on a zero-length PADDED frame the ORIGINAL NULL-derefs and PANICS (6,916 SIGSEGV-confirmed traps) while the twin does a 1-byte HEAP OVER-READ that SUCCEEDS and CONTINUES with an attacker-influenced pad_len (ASan: "READ of size 1 ... 0 bytes to the right of 9-byte region"). So dropping -DRUST_HTTP2_FRAME trades a deterministic panic for a SILENT adjacent-heap read: **the flag restores the C implementation, NOT the pre-port behavior**. Resolution = DOCUMENTED, not fixed: a faithful twin is not expressible without also reverting the caller's contiguous-buffer assembly, and the over-read is retained deliberately because it is what lets the differential witness advisory 0010's missing bound. Now stated at the dispatcher (net/http2.c) and in the FRAME RE-REPRESENTATION note in net/http2_frame_c.c, whose header no longer calls itself a "verbatim-faithful extraction". Shipped impact: NONE (live is Rust). NOT BUILT/BOOTED (isolated parallel-dev). |
| gui/theme_parser.c theme_parse_ini line tokenizer (the untrusted theme .ini line-record atom: read ONE logical line + classify {skip | [section] | key=value} with every fixed output field hard-capped; #141 downloadable/editable themes) | b822 | ~40 (theme_parse_line_c reuses the existing verbatim static classifiers is_comment_or_empty/parse_section_header/parse_key_value; the file-loop body is exposed as one (bytes,len)->record step; the theme_parse_ini loop now calls the seam) | ~150 (theme_parse_line_rs + theme_parse_line_impl + skip_ws/trim_trailing/parse_section_header/parse_key_value + ThemeLine #[repr(C)] sizeof asserted ==264; slice of exactly len, every out field hard-capped) | boot [RUST-DIFF] theme_parse 9 KAT vectors (incl embedded-NUL, over-long key, unterminated section) rs==c PASS + OFFLINE 33.9M (1.30M well-formed + 32.6M malformed) 0 mism; C fully bounded (ASan-clean 32.6M) | boot [RUST-PERF] theme_parse C=2158 -> RS=2500 cyc/file (RDTSC; RS ~1.16x, theme parse is a cold once-per-theme-load path) | **NO reachable OOB (LATENT/DEFENSE-IN-DEPTH, like url/dns/arp).** The verbatim C already caps every fixed output field (section 64 / key 64 / value 128 / line 256) so no reachable overflow (ASan-clean over 32.6M malformed). Rust removes the raw-pointer-scan class by construction (slice of exactly len, saturating accessors, index-checked writes). One rs/c divergence found + fixed OFFLINE: an EMBEDDED interior NUL ends the classified line in C (the classifiers scan the NUL-terminated line[] with while(*p)); a naive Rust slice treated it as data - fixed by ending the classified view at the first interior NUL (consumed still counts the full bytes). NOT an advisory. | LIVE under -DRUST_THEME_PARSE (theme_parse_ini -> objdump: call <theme_parse_line_rs>; theme_parse_line_c retained for rollback + differential). Throwaway VM2931 (same setup as jpeg row): [RUST-DIFF] theme_parse PASS (9 KAT) + [RUST-SEC] latent def-in-depth + [RUST-PERF] C=2158/RS=2500; all 30 [RUST-DIFF] PASS (no regression). kernel.elf md5 46d985caaaea681473c5b5e0b0e9a896. |
| media/wav.c wav_parse_header (RIFF/WAVE header-parse seam: RIFF/WAVE magic + chunk id/size walk + fmt/data field reads + WAVE_FORMAT_EXTENSIBLE subformat + data-size-vs-buffer clamp + PCM/params validation -> WavInfo; the PCM sample decode/convert STAYS C behind it) | b823 | ~55 (the wav_create RIFF walk + validation lifted VERBATIM into wav_parse_header_c; wav_create now fills a WavInfo via the seam and maps rc -> the original kprintf diagnostics) | ~150 (wav_parse_header_rs + wav_parse_walk + WavInfo #[repr(C)] sizeof asserted ==20; slice of exactly len, every chunk-size/fmt-length a bounds-checked slice index, checked_add chunk advance) | boot [RUST-DIFF] wav 11 vectors (valid 8/16/24/32-bit + EXTENSIBLE->PCM + float/MP3 reject + huge-data clamp + odd-channel + too-short + bad-magic; WavInfo compared FIELD-BY-FIELD not memcmp - tail padding differs C-vs-Rust) 0 mism + OFFLINE differential+ASan/UBSan (wav_diff/wav_perf) clean per the wav agent | RS/C ~parity (cold once-per-file open path) | **LATENT / DEFENSE-IN-DEPTH - NO reachable OOB.** The live wav_create walk was ALREADY bounded (every fmt/data read guarded by body+N<=size, the data clamp caps data_size to the buffer). Rust removes the raw-pointer-walk class by construction (slice of exactly len, checked_add chunk advance). NO advisory. | LIVE under -DRUST_WAV_PARSE (wav_create -> wav_parse_header dispatcher -> wav_parse_header_rs; wav_parse_header_c retained for rollback + differential; PCM decode/convert/seek STAY C). Verified throwaway VM2823 (fresh 1G LV copy of golden b822, b823 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64, own efidisk; destroyed + LVs purged): [RUST-DIFF] wav 11 vectors mism=0 MATCH (LIVE=rust) + [RUST-SEC] wav def-in-depth; healthy desktop DESKTOP_READY + 2 differing screendumps (color-cycling screensaver, live) + idle RIP=0x58de7a CPL=0 HLT=1 stable + heartbeat idle:99; all 33 [RUST-DIFF] PASS (0 FAIL = no regression). HONEST: on-desktop WAV PLAY not triggered headlessly (iMac-USB-HID kernel ignores headless QMP input, no boot-time WAV decode), so live proof = boot self-test on the REAL compiled seam + objdump routing (wav_create -> call <wav_parse_header_rs>). Folded to golden b823 (old md5 97585189 -> new 21fab7fa, all 4 golden ESP paths 530c6f6c) + VM2200 re-verified (DESKTOP_READY, idle HLT, 2 differing screendumps, wav MATCH LIVE=rust). Golden pre-fold snapshot NAS:/mnt/pve/NFS_raid5/golden-snapshots/maytera-imac-b738-pre-b823-97585189f6be8a7bab169646883c2cfd.img (md5-verified). kernel.elf md5 530c6f6c16a441f1ccec80dcf9a45e47. |
| net/tls/cert_store.c base64_decode (PEM->DER base64 decode seam: the decode loop over the untrusted base64 body of a PEM CERTIFICATE; the -----BEGIN/END----- strstr framing scan STAYS C in cert_parse_pem) | b823 | ~22 (base64_decode body kept VERBATIM as base64_decode_c; base64_decode is now a one-line dispatcher) | ~70 (cert_base64_decode_rs; slices span exactly in_len/out_cap, every read+write bounds-checked, wrapping u32 accumulator) | boot [RUST-DIFF] cert_b64 (48-byte synthetic DER round-trip, c_len==rs_len==48, byte-identical, memcmp vs original plaintext) MATCH + [RUST-PERF] cert_b64 RDTSC + OFFLINE 1.5M-vector differential 0 mism, round-trip OK, ASan clean per the cert_b64 agent | RS/C ~1.45x (cold per-certificate path) | **LATENT / DEFENSE-IN-DEPTH - NO reachable OOB; plus a MINOR UB hardening.** The C base64_decode is ALREADY output-bounded (`written < out_len` loop guard); Rust keeps that AND makes the bound structural (slice). Additionally the C `acc = (acc << 6) | val` on a signed int is a benign signed-shift UB (CWE-190-class) that fires on every real certificate; the Rust wrapping-u32 accumulator well-defines it, byte-identical low bits. NO advisory (no memory corruption). | LIVE under -DRUST_CERT_B64 (cert_parse_pem -> base64_decode dispatcher -> cert_base64_decode_rs; base64_decode_c retained for rollback + differential; PEM framing + DER parse STAY C). Verified throwaway VM2823 (same setup as wav row): [RUST-DIFF] cert_b64 c_len=48 rs_len=48 MATCH (LIVE=rust) + [RUST-SEC] cert_b64 def-in-depth + [RUST-PERF] cert_b64 RDTSC; all 33 [RUST-DIFF] PASS (no regression). HONEST: a live HTTPS handshake decoding a real PEM through the seam was NOT triggered headlessly this run; live proof = boot self-test on the REAL compiled seam + objdump routing (base64_decode dispatcher -> call <cert_base64_decode_rs>). Folded to golden b823 + VM2200 re-verified (cert_b64 MATCH LIVE=rust). kernel.elf md5 530c6f6c16a441f1ccec80dcf9a45e47. |
| fs/xattr.c xattr entry-walk (on-disk /.xattr/*.xat per-attribute record iterator: 8-byte packed header read + name/value extent derive, shared by xattr_get/xattr_list; the reachable sys_getxattr/sys_listxattr read paths) | b823 | ~30 (the two inline get/list walks replaced by the seam; the verbatim over-reading walk kept as fs/xattr_entry_c.c for rollback + differential) | ~90 (xattr_entry_next_rs + XattrEntry #[repr(C)] sizeof asserted ==16, xattr_entry_header_t asserted ==8; slice of exactly len, header/name/value extents each checked_add'd and gated against len -> rc -1 on would-be-OOB) | boot [RUST-DIFF] xattr_entry (well-formed 2-entry block, descriptor compared FIELD-BY-FIELD) 0 mism + [RUST-SEC] evil lone-header huge name_len/value_len: C rc=1 (offsets past block, caller over-reads) vs Rust rc=-1 (confined) + OFFLINE 2.25M well-formed 0 mism + 2.0M malformed rscheck + C ASan heap-buffer-overflow over the malformed corpus per the xattr agent | RS/C ~1.35x (cold per-xattr-op path) | **REACHABLE OOB (CWE-125) = MAYTERA-SEC-2026-0011, Medium, LOCAL.** A crafted/corrupt FAT image's /.xattr/*.xat block declares name_len/value_len/attr_count past the buffer; the live C get/list walk advances by those unchecked on-disk lengths so attr_name/attr_value point past the kmalloc'd file buffer -> strcmp/memcpy heap over-read (info-leak/DoS), reachable via sys_getxattr/sys_listxattr. The Rust seam rejects (rc != 1) BEFORE any name/value is dereferenced, confining the class by construction. C-fallback hardening ticket #508. Affected <= b822, patched b823 (get/list routed under -DRUST_XATTR). | LIVE under -DRUST_XATTR (xattr_get + xattr_list -> xattr_entry_next dispatcher -> xattr_entry_next_rs; xattr_entry_next_c retained in fs/xattr_entry_c.c for rollback + differential; xattr_set/xattr_remove write walks still C = #508 scope). Verified throwaway VM2823 (same setup as wav row): [RUST-DIFF] xattr_entry 2 entries mism=0 MATCH (LIVE=rust) + [RUST-SEC] xattr_entry confinement WITNESSED (evil lone-header huge name_len/value_len -> c rc=1 offsets-past-block, rust rc=-1 confined); all 33 [RUST-DIFF] PASS (no regression). INTEGRATION HICCUP FOUND + FIXED: the provided boot self-test `good[]` vector declared entry0 name_len=6 for a 5-byte "user\0" name (off-by-one), desyncing the walk so the second pseudo-entry was mid-header garbage that the confinement seam correctly rejected (rc=-1) while C over-read (rc=1) -> first build's xattr line was mism=1 MISMATCH; corrected the vector to name_len=5 (the SEAM was never wrong - the differential proved the confinement working). HONEST: on-disk xattr get/list not triggered from Ring-3 headlessly; live proof = boot self-test on the REAL compiled seam + objdump routing (xattr_get + xattr_list -> call <xattr_entry_next_rs>). Folded to golden b823 + VM2200 re-verified (xattr_entry MATCH LIVE=rust). MAYTERA-SEC-2026-0011 (CWE-125, Medium, LOCAL) filed; C-fallback hardening #508. kernel.elf md5 530c6f6c16a441f1ccec80dcf9a45e47. **CORRECTION 2026-07-16 (3-way drift audit + FIX): THE PARTTBL BUG CLASS RECURRED HERE, and this row's rollback claim was FALSE.** `xattr_entry_next_c` was NOT a faithful lift: the ORIGINAL (fs/xattr.c:385-397 pre-b823) advanced a **64-bit pointer** guarded by `ptr < end`, so a crafted name_len/value_len near 2^32 put ptr FAR past end and the walk TERMINATED; the twin computed a `uint64_t next` and then **TRUNCATED** it into its u32 cursor (`*pos = (uint32_t)next;`), which **WRAPPED and kept walking** from a bogus low offset (value_len=0xFFFFFFFF, p=0, name_len=0 -> next=0x100000007 -> *pos=7). Measured **86,742/600,000 (14.5%) ORIGINAL-vs-TWIN mismatches, 100% of them wraps, 0 other causes**. Byte-identical on WELL-FORMED input (0/150,000 blocks, 484,583 entries) and wrong on exactly the hostile input the seam exists for; structurally invisible to the rs-vs-c differential (Rust rejects those inputs, so rs!=twin was expected-and-ignored and nothing ever compared twin to the original). The twin's header claimed it "reproduces EXACTLY what the live C did, INCLUDING the missing bounds" - false, and now corrected in-file. **FIXED: clamp, do not truncate** (`*pos = (next > (uint64_t)len) ? len : (uint32_t)next;`), which reproduces the original's termination exactly because every cursor >= len re-enters the `p >= len` guard: **86,742 -> 0 over 1.35M vectors** (600k + 5 seeds x 150k), wrap coverage ~29.5k/seed so the boundary is genuinely exercised, well-formed suite still 0, and 5/5 negative controls FIRE (incl. one that re-introduces the truncation: 21,633/150k). `name_off`/`value_off` are deliberately NOT clamped to `len` (they are consumed as ADDRESSES and the original really did hand out a past-the-block pointer = advisory 0011's OOB read #2; clamping them would have invented NEW drift) and are guarded only at the u32 representation boundary. Rollback fidelity RESTORED: ASan+UBSan with the Rust linked under `setarch -R` on exact-size allocations shows ORIGINAL and TWIN with IDENTICAL OOB-report counts (3/3) and RUST 0, i.e. the twin still reproduces the original's over-read and the clamp did not accidentally confine it. Shipped impact: NONE (golden ships -DRUST_XATTR; the Rust cannot truncate). NOT BUILT/BOOTED (isolated parallel-dev). |
| media/flac.c (FLAC parse) | - | - | - | ASSESSED, NOT INTEGRATED | - | - | flac: assessed, NOT integrated - vendored dr_flac owns FLAC parsing; the flac seam is an ADDITIVE shadow-parser (not a strangler of our C) and a stricter Rust pre-gate risks false-rejecting valid .flac; hardening dr_flac is a separate effort. Left in the kernel build container:/root/rust-parallel/flac_parse/ unintegrated. |

| crypto/ed25519.c unpack25519 (TweetNaCl 32-byte compressed-point y-decode into gf[16] + sign-bit mask; curve sqrt/scalarmult/field math STAY C) | b824 | ~4 (unpack25519 kept VERBATIM as unpack25519_c for rollback + differential; the live unpack25519 is now a static-inline dispatcher) | ~10 (unpack25519_rs; slices of EXACTLY 32/16 bound every index, no #[repr(C)] struct crossed) | boot [RUST-DIFF] ed25519_decode rs==c PASS (4 edge vectors {0, all-0xff, p, sign-bit} + 20000 PRNG vectors, mism=0) + OFFLINE 9,048,584 unit + 203,015 full-verify-parity vectors 0 mism, RFC8032 KAT accept + tamper/malformed reject, ASan 0 OOB on C decode+verify | boot [RUST-PERF] ed25519_decode C=3766412 rs=2531516 cyc/100k (RS ~1.5x FASTER; cold per-signature path) | **LATENT / DEFENSE-IN-DEPTH - NO reachable OOB (fixed-size 32-byte read).** unpack25519 reads exactly n[0..32] into gf[16]; the C is already bounded. Rust removes the raw-index class by construction (slices of exactly 32/16). NO advisory. | LIVE under -DRUST_ED25519_DECODE (unpackneg -> unpack25519 dispatcher -> objdump: 5 call sites to <unpack25519_rs>; unpack25519_c retained for rollback + differential; curve math STAY C). INTEGRATION HICCUP FOUND + FIXED via throwaway: the provided selftest's ed25519_rdtsc() used `rdtscp`, which the golden cpu=kvm64 does NOT expose -> #UD Invalid Opcode -> KERNEL PANIC at boot (RIP 0x5aaa60); changed to `lfence; rdtsc` (base ISA). Throwaway VM2824 (fresh 1G LV copy of golden b823, b824 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64, own efidisk copied from VM2200): [RUST-DIFF] ed25519_decode PASS mism=0 LIVE=rust; DESKTOP_READY + autologin OK + idle RIP=0x58e88a CPL=0 HLT=1 (twice stable) + heartbeat flips progressing idle:99; all 35 [RUST-DIFF] PASS (0 FAIL = no regression, b823 had 33). kernel.elf md5 90e9c24d8101d045f834af99a8ade34f. |
| net/rpc.c XDR DECODE primitives (bounded XDR decode cursor + typed reads that pull u32/u64/opaque/bytes/string/string_len/skip/nfs_fh3 out of untrusted XDR-encoded RPC/NFS server replies #317; the 4-byte length-prefix + 4-byte-align round-up seam; the ENCODE branch stays C untouched) | b824 | ~85 (the 8 decode branches kept VERBATIM as xdr_*_c twins for the differential) | ~230 (xdr_decode_*_rs + #[repr(C)] XdrT sizeof-locked 32 offsets 0/8/16/24/25 + checked_add on every pos advance AND the align round-up; every source read a slice bounded to size) | boot [RUST-DIFF] xdr decode NFS-reply pos=96/96 mism=0 PASS (real NFS LOOKUP3resok-shaped reply decoded via LIVE path AND the _c twins, compared field-by-field + cursor-exact) + OFFLINE 7,500,000 fuzz rs==c 0 hard mismatch + xdr_c ASan+UBSan CLEAN over the malformed corpus + align-overflow class single-shot: C ASan negative-size-param in memcpy vs Rust CONFINED(reject) | boot [RUST-PERF] xdr decode 203 cyc/full-reply | **LATENT / DEFENSE-IN-DEPTH - NO reachable OOB on the 64-bit target.** The C decode is already SOURCE-bounded: every opaque/string length is a 4-byte XDR field (u32), and on 64-bit size_t (len+3)&~3 cannot wrap, pos+aligned cannot overflow, and pos+aligned>size correctly rejects. Rust confines the align round-up overflow class by construction (checked_add). NO advisory for the seam. **SEPARATE finding: net/nfs.c nfs_read passes an attacker-controlled u32 data_len straight to xdr_opaque(reply,buffer,data_len) with NO clamp to the destination = a remote DESTINATION over-WRITE (MAYTERA-SEC-2026-0012, CWE-787, ASan-proven: WRITE of size 2000 0 bytes past a 512-byte region). The source-bounded XDR seam does NOT confine it; fixed directly in nfs.c (clamp data_len<=count). C-hardening ticket #509.** | LIVE under -DRUST_XDR (xdr_uint32/uint64/opaque/bytes/string/string_len/skip/nfs_fh3 decode branches -> objdump: call sites to <xdr_decode_uint32_rs>x11/uint64x3/opaquex2/bytesx2/stringx2/string_lenx2/skipx3/nfs_fh3x2; encode branch + all RPC/NFS message logic unchanged; xdr_*_c twins retained for rollback + differential). INTEGRATION HICCUP FOUND + FIXED via throwaway: the provided selftest reply builder wrote the final field as a length-prefixed opaque (PU32(5);"HELLO") but the decoder read a bare fixed-5 opaque -> pos=96/100 FAIL (mism=0, i.e. rs==c AGREED - a TEST-VECTOR bug, not a seam bug, analogous to the b823 xattr vector hiccup); corrected the builder to a bare 5-byte opaque -> pos=96/96 PASS. Throwaway VM2824 (same setup as ed25519 row): [RUST-DIFF] xdr PASS mism=0 LIVE=rust; DESKTOP_READY + idle HLT stable; all 35 [RUST-DIFF] PASS (no regression). kernel.elf md5 90e9c24d8101d045f834af99a8ade34f. |
| net/ssh/ssh_transport.c ssh_recv_packet (SSH binary-packet framing underflow) | - | - | - | ASSESSED, NOT INTEGRATED (dead scaffolding) | - | - | The batch-3 SSH framing seam targeted ssh_recv_packet's size_t payload_len=packet_len-1-pad_len underflow. On integration ssh_transport.c was found to be DEAD SCAFFOLDING (Makefile NET_SSH_SOURCES = only ssh2.c + ssh2_server.c; ssh_recv_packet has ZERO callers, only a decl in ssh.h). The LIVE ssh2.c ssh2_extract() already rejects the underflow in BOTH branches via signed-int `int payload_len=(int)packet_length-pad-1; if(payload_len<1) return -1;`. NOT reachable in the shipping kernel -> NO advisory, NO seam integrated (assessed-clean). Retargeting to ssh2.c would be a new unverified port; batch-3 stops the port. Seam left in the kernel build container:/root/rust-parallel/ssh_frame/ unintegrated. |
| gui/taskmanager.c taskmgr_core (perf_ring_stats + taskmgr_sort_rows: the Task Manager Performance-tab metric-ring aggregation + the process-row sort permutation, #487/#349) | b825 | ~40 (perf_ring_stats_c + taskmgr_sort_rows_c kept VERBATIM in-file as the reference twins + rollback; the live entry is a #ifdef RUST_TASKMGR_CORE dispatcher) | ~120 (perf_ring_stats_rs + taskmgr_sort_rows_rs; #[repr(C)] PerfStat sizeof-locked 20, ProcKey sizeof-locked 12; cap-exact slices bound every index, count>cap / cap==0 / head>=cap rejected -> -1 before any access) | boot [RUST-DIFF] taskmgr_core 272 vecs mism=0 MATCH (LIVE=rust) over cap 1..16 x head, perf_stat compared field-by-field + sort permutation compared index-by-index + [RUST-SEC] taskmgr_core sort count>cap rust rc=-1 (confined, C over-writes) / ring cap0 rust rc=-1 (confined, C div0) + OFFLINE 5,000,000 differential (3M perf_ring + 2M sort) 0 mism, ASan witnessed the C CWE-787 OOB-write + UBSan the div-by-0 | offline micro-bench perf_ring C=241 RS=294 cyc/op (1.22, ~parity) / sort(64) C=10215 RS=5061 cyc/op (0.50, RS ~2x faster; memcpy-idiom/inlining, not language) | **DEFENSE-IN-DEPTH - NEW data core, NO shipping C consumer feeds the unsafe inputs -> NO advisory.** The naive C sort over-WRITES its permutation array when count>cap (CWE-787) and the naive ring divides by zero when cap==0; the Rust rejects both by construction (rc -1 before access). But the ONLY live callers are gui/taskmanager.c's own draw paths which never pass count>cap or cap==0, so neither is reachable today. Kept confined-by-construction so a future careless caller cannot regress it. NO advisory filed (per the dev agent's defense-in-depth call). | LIVE under -DRUST_TASKMGR_CORE (gui/taskmanager.c perf_ring_stats/taskmgr_sort_rows macros -> _rs; objdump taskmanager.o: PLT32 relocs to perf_ring_stats_rs x2 + taskmgr_sort_rows_rs x4; the _c twins retained in-file for rollback + differential). Self-test uses rdtsc-free timing (no [RUST-PERF] block) so no rdtscp #UD on kvm64 (the b824 lesson heeded). Throwaway VM2825 (fresh 1G LV copy of golden b824, b825 to all 4 ESP paths, USB-MSC xHCI q35/OVMF/kvm64, efidisk copied from VM2200): [RUST-DIFF] taskmgr_core 272 vecs mism=0 MATCH (LIVE=rust) + [RUST-SEC] confinement WITNESSED (sort count>cap rust rc=-1, ring cap0 rust rc=-1); desktop rendered with "v1.95.0 (build 825)" on screen; 37 distinct [RUST-DIFF] all PASS (b824 had 35, +taskmgr_core +the http2 LIVE=rust status line = 37, 0 FAIL = no regression); idle RIP=0x59245a CPL=0 HLT=1 (twice) + RAX churn + heartbeat advancing (compositor presenting). HONEST: this iMac-USB-HID image ignores headless QMP pointer/keyboard input, so the Task Manager WINDOW open + four-tabs + column-sort + right-click End-Task/priority interaction was NOT driven headlessly and is left for the user's real-mouse test; live proof = boot self-test on the REAL compiled seams (perf_ring_stats_rs/taskmgr_sort_rows_rs execute in the live kernel) + objdump routing (taskmanager.o PLT32 -> _rs) + desktop render. Folded to golden b825 (new golden md5 b3cfc8c4720990ebba58931a8d664539; b824 golden f0cfcb37 NAS-snapshotted pre-b825, md5-verified) + VM2200 re-verified (taskmgr_core MATCH LIVE=rust, build 825 on-screen, 2 differing screendumps, idle HLT). kernel.elf md5 b6e5858e899d2576c7e39c0dce9ad52b. |
| fs/ext2.c parttbl (MBR + GPT partition-table parse: the GPT header validate + per-sector partition-entry-array walk + the MBR 0x83 fallback, i.e. the pure byte-walking of ext2_find_partition, #365 root discovery; blk_read I/O + control flow STAY C) | b826 | ~50 (the GPT header guard, the entry walk and the MBR scan kept VERBATIM as parttbl_gpt_hdr_c / parttbl_gpt_sec_scan_c / parttbl_mbr_find_c for rollback + differential; the live entries are static-inline #ifdef RUST_PARTTBL dispatchers; ext2_find_partition itself keeps its blk_read loop and now calls the seam) | ~150 (parttbl_gpt_hdr_rs / parttbl_gpt_sec_scan_rs / parttbl_mbr_find_rs; slices of EXACTLY `len` bound every disk-controlled index, e*esz is a checked_mul, 2 x #[repr(C)] structs sizeof-locked at 24 / 16) | boot [RUST-DIFF] 24,745 vecs 0 mism (well-formed GPT x every legal esz 128..512 x every entry slot + header-boundary sweep num 0..260 / esz 120..520 + FirstLBA-truncates-to-0 fallback + 8k random fuzz), VERIFIED on the b826 boot; OFFLINE 3-way 40,921 vecs 0 mism AND 0 extraction drifts vs the UNTOUCHED original (negative control confirmed the oracle fires) | IN-KERNEL (authoritative, b826 boot): gpt_sec_scan C=321 -> RS=205 cyc/op ratio=0.63 (~1.6x). NOTE the offline host-cc figure was ratio=0.16 (~6x); the real in-kernel win is much smaller, and this is a COLD path (once per boot/disk probe) so it is irrelevant to system perf either way. The host-cc win was rustc autovectorizing the 16-byte GUID compare where gcc stays scalar: a CODEGEN artifact, not a language one | DEFENSE-IN-DEPTH, no live bug, NO advisory. The C is genuinely BOUNDED on the live path: ASan+UBSan clean over 40,921 vectors AND an exhaustive 98,560-combination (esz 128..512 x num 1..256) guard-legal probe. Safety rests ENTIRELY on one load-bearing guard (esz >= 128 with per_sec derived as 512/esz => max touch 512-esz+39 <= 423 < 512); bypassing it ASan-proves an instant heap-buffer-overflow at the GUID read. Rust removes that dependency BY CONSTRUCTION (slice bounds + checked e*esz), so the OOB class cannot return if the guard is ever edited | LIVE; routing proven by objdump (ext2_find_partition -> `call 5fe460 <parttbl_mbr_find_rs>` / `call 5fe1f0 <parttbl_gpt_hdr_rs>` / `call 5fe290 <parttbl_gpt_sec_scan_rs>`); verified throwaway VM2960 (destroyed) + golden fold + VM2200, desktop "v1.95.0 (build 826)" |
| drivers/usb_audio.c uac_parse_config (USB Audio Class configuration-descriptor walk: the INTERFACE / CS_INTERFACE FORMAT_TYPE / ENDPOINT descriptor scan that selects the isochronous OUT endpoint, preferring 16-bit PCM; the xHCI control transfers, SET_INTERFACE/SET_CONFIGURATION and the kprintf report STAY C) | b826 | ~45 (the walk kept VERBATIM as uac_parse_config_c, writing through a pure usb_uac_sel_t out-struct instead of the file-static `uac`, for rollback + differential; the live entry is a static-inline #ifdef RUST_USB_DESC dispatcher; uac_parse_config now copies the seam result into `uac` and keeps the kprintf) | ~90 (uac_parse_config_rs + #[repr(C)] UsbUacSel sizeof-locked at 24; d.get(k) returns None exactly where the C would leave the len-byte buffer) | boot [RUST-DIFF] usb_desc 2,000 well-formed vectors 0 mism, VERIFIED on the b826 boot (well-formed ONLY by design: on malformed input the two DELIBERATELY diverge, since the C over-reads and keeps walking while the Rust stops, so counting a "mismatch" there would be counting the fix as a defect); OFFLINE 200,000 well-formed vectors 0 mism + 300,000 malformed vectors (100,188 = 33.40% would over-read in the C, max 3 bytes past len; 0 in the Rust) | not micro-benched: enumeration-time cold path, runs once per USB audio device plug | DEFENSE-IN-DEPTH, NO advisory, but the over-read is REAL and REACHABLE, not latent: the C reads cfg[i+2], cfg[i+3], cfg[i+6] (INTERFACE) and cfg[i+2..i+6] (ENDPOINT) guarded ONLY by `blen >= 2`, so a crafted descriptor with a lying 2-byte INTERFACE/ENDPOINT reads up to 5 bytes past `len`. VERIFIED REACHABLE at the buffer edge by a crafted vector (a device reporting wTotalLength >= 512 clamps to total = 512 = sizeof(cfg) in drivers/xhci.c:2252, and a lying 2-byte INTERFACE at offset 510 makes the C read cfg[512], 1 byte past the 512-byte static buffer). NO advisory because impact is negligible, NOT because it is unreachable: cfg is a STATIC .bss array so the over-read lands in adjacent .bss (no page boundary, no fault), the bytes only influence which endpoint is selected plus one kprintf'd bInterval, and it needs a malicious USB device physically attached. CORRECTION for the record: the dev harness's "0 of them with len==512" is a property of its RANDOM CORPUS, NOT a proof of unreachability - a crafted vector reaches it. Rust removes the class BY CONSTRUCTION | LIVE; routing proven by objdump (uac_probe -> `call 6000f0 <uac_parse_config_rs>`, uac_parse_config inlined); verified throwaway VM2960 (destroyed) + golden fold + VM2200 |
