[package]
name = "maytera_rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[profile.release]
panic = "abort"
lto = true
opt-level = "z"

[dependencies]
# No std library dependencies for bare metal
