/* Vendored-Tremor math.h shim. The integer decode path calls NO libm function
 * (verified: all sin/cos/sqrt/exp/floor/rint uses are comments or integer
 * lookups). Only M_PI is referenced via os.h, defined there if absent. */
#ifndef _MAYTERA_TREMOR_MATH_H
#define _MAYTERA_TREMOR_MATH_H
#endif
